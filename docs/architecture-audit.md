# Architecture Audit — Drift, Divergence, and Redundancy

> **Audited:** 2026-08-24 · **Against:** v0.7.5 working tree (package 0.7.4)
>
> **Status: HISTORICAL NARRATIVE — do not edit, do not track work here.**
> This file is the evidence and reasoning behind each finding, kept because the
> *why* is what stops a fix being re-filed as drift. Live tracking moved out on
> 2026-08-26:
>
> - **`docs/audit-register.md`** — the tracker: every item, its state, its id.
> - **`docs/device-validation.md`** — the on-device steps each fix depends on.
>
> Five findings in this file were re-derived against the tree on 2026-08-26 and
> **turned out to be wrong or incomplete in ways that change decisions.** They
> are recorded in "Corrections" at the foot of this document. Read that section
> before acting on §1.2, §2.8, §2.9 or register item 4b.

This is the detailed record of a three-lens adversarial review: (1) a general correctness/dead-code pass, (2) intended-design vs current-state drift, and (3) store-logic drift and redundancy. It is the source material for the remediation register at the end. The companion guard is the `unifideck-drift-guard` skill plus `scripts/validate_architecture.py`, which machine-enforce the drift classes found here.

Two facts frame every section:

1. **The code is ahead of its documentation by a full store and roughly a full release.** Battle.net is implemented but every doc and skill still says "five stores." Version strings disagree (§2.6).
2. **There is no single agreed layer model or store-type model.** Four incompatible layer maps exist, and the wrapper/CLI distinction is maintained in two separate hand-written tables with no linking field.

---

## Part 1 — General adversarial review

### 1.1 P0 correctness bugs (silent failures)

1. ~~**`GAME_INSTALLED` payload contract mismatch.**~~ **RESOLVED — and the original diagnosis was wrong.** The finding claimed the emitter at `core/manifest.py:463-470` sent `game_id` while both subscribers read `app_id`, killing artwork-on-install and Proton-on-install "despite the event firing". Validation showed the event **never fired at all**, and that fixing the payload would have changed no behaviour:

   - The only emit site was `_scan_one_root` inside `discover_all`, whose two wrappers (`discover_installed_games`, `discover_and_log`) had zero callers — their only references were `vulture_whitelist.py` entries. The dead code had already been noticed and silenced rather than deleted.
   - Artwork-on-install was **redundant, not missing**: a game's shortcut and its cover art are created at *sync* time, and installing only flips that shortcut's install tag while preserving its appid (`services/shortcut/events.py:163-178`). `download/worker.py` documents the same reasoning where it declines to emit.
   - Proton-on-install had **four** independent reasons it could never work: no live emitter; the payload key mismatch; `DEFAULT_TOOLS` empty for all six stores by design; and in the plugin the writer was pointed at `localconfig.vdf` (`bootstrap/paths.py:166`, re-bound by `steam/current_user.py`) while `CompatToolMapping` lives in `config/config.vdf`.
   - **The real user-facing bug was on the frontend and this audit missed it.** `src/lib/steam-bridge/collection-manager.ts` subscribed to `GAME_INSTALLED` to rebuild the `[Unifideck] Installed` collection. That never fired, while `GAME_UNINSTALLED` on the next line did — so the collection (and any TabMaster tab on it) dropped games on uninstall but never picked them up on install until the next full sync. Its own comment claimed to have fixed exactly that.
   - Both guards locked in **opposite halves** of the mismatch and so caught nothing: `tests/unit/test_proton_ge.py` asserted the subscriber's `app_id` contract while `scripts/validate_event_schemas.py` asserted the emitter's `game_id` contract.

   Fix: repointed the collection manager at `SHORTCUT_INSTALL_STATE_CHANGED` (the event that actually fires, in both directions), then deleted the phantom event and everything hanging off it — both dead subscribers, the dead discovery half of `core/manifest.py` (520→257 lines), the entire ProtonService compat-tool path (243→120 lines, resolving half of §1.4), and the two `vulture_whitelist.py` entries. `Events.GAME_INSTALLED` no longer exists, so nothing can subscribe to it again.

   **Lesson for the rest of this register:** "emitted but with the wrong payload" and "not emitted at all" look identical from a grep of emit sites. Check that the *enclosing function* has callers before trusting an emit site, and check whether a dead subscriber's job is already done elsewhere before restoring it.

2. ~~**`TOAST_NOTIFICATION` is emitted but consumed nowhere.**~~ **RESOLVED — the finding held, and the impact was larger than stated.** All three emit sites are live-reachable (checked the enclosing functions per §1.1.1's lesson): `emit_circuit_open_toast` ← `check_circuit_breaker` ← `LauncherService.launch`; `emit_launcher_error_toast` ← `handle_launcher_error` ← the same `launch`; `_emit_write_refused` ← `_save_shortcuts`'s write guard. Three independent reasons none of them ever reached a user:

   - **No consumer**, as the audit said: absent from `src/types/events.ts`, absent from `WATCHED_EVENTS`, no Python subscriber.
   - **Wrong process — the audit missed this, and it is the load-bearing half.** `LauncherService` is built *only* by `launcher/bootstrap.py:69`, the out-of-process launcher. Its bus dies with the process, and `frontend_bridge.install_bus_forwarder` mirrors `LAUNCHER_STAGE` **and nothing else** into the bridge file. Adding a frontend subscriber alone would have fixed nothing for the two launcher toasts. (`ShortcutService` is built in *both* processes — `build_service_subset` includes `"shortcut"` — so its toast needed both legs to work.)
   - **Payload key mismatch**: emitters sent `params=`, both renderers read `i18n_params`. Fixing the first two alone would have rendered `"Couldn't launch  (). Check the logs for details."` — the same class of failure as §1.1.1's `app_id`/`game_id` split.

   The i18n strings existed and were translated in all 16 locales the whole time; only the delivery channel was dead.

   **What the user actually experienced.** A circuit-breaker refusal was *completely* silent: the toast was dead, and `CIRCUIT_STATE_CHANGED` sits in `WATCHED_EVENTS` with zero frontend subscribers (the `useCircuitState` hook and `PlayButtonOverride` badge its docstring describes were never built), and `clear_launch_failures` / `arm_circuit_bypass` have no frontend caller (§1.2). So after 3 failures in 10 minutes the user pressed Play, got a flicker, landed back on the game page with no message, no badge, and no way to reset — reading as "the plugin randomly stopped launching my game." Terminal `LauncherError`s were equally silent (the dispatcher maps the `Result` to an exit code and exits; Steam shows nothing for a non-Steam shortcut exiting non-zero). And a shortcut write refusal left the sync reporting success while the library silently did not change — the exact silence `_emit_write_refused`'s docstring exists to prevent.

   Fix: routed all three through `LAUNCHER_STAGE` — the one channel wired on both delivery legs — then deleted `Events.TOAST_NOTIFICATION` so nothing can emit into it again, along with its `validate_event_schemas.py` row and its arm of `check_hardcoded_strings.py`'s `PY_BUS_EMIT` regex. Added `duration_ms` to `emit_stage`/`launcher_toast` and taught both renderers to honour it via a shared `src/services/toast-duration.ts` (the write-refusal paragraph asks for 12s and was being cut at 7.5s). The two launcher toasts now name the game via the existing `launcher/game_title.resolve_title`, fed into the `{{game_key}}` placeholder the strings already declare — zero locale churn. Dropped the "Show logs" action (see the new register items) and with it `circuit_breaker.get_launch_id_or_none`, whose only purpose was to build it. New `tests/unit/test_toast_channel_migration.py` asserts each emitter against the **consumer's** contract, not the emitter's.

3. ~~**`MetricsCollector` timers/gauges are never wired.**~~ **RESOLVED before this register was worked, by commit `8b62be5`** ("use @subscribe decorators for metrics…"). The finding was accurate: `auto_wire(self, self._bus)` and its `logger.info` sat inside the `for event, name in counter_events:` loop (confirmed at `8b62be5^:core/metrics_collector.py:96-100`), so they ran 7 times, and the timer/gauge `_on_*` handlers carried no `@subscribe` metadata. Verified fixed in the current tree: `auto_wire` runs once outside the loop (`metrics_collector.py:104`) and every `_on_*` handler is decorated. `tests/unit/test_metrics_collector.py` pins both halves. The register line was simply never ticked.

4. ~~**Double emission of `DOWNLOAD_*` with incompatible payloads.**~~ **RESOLVED — and the audit named the wrong symptom.** The mechanism was as described: for Epic/Amazon every phase emitted twice, once in the `DownloadWorker` shape (`item=`, plus `game=` on completion) and once in a flat store shape (`store=`, `game_id=`) from `epic/install.py` and `amazon/amazon_install.py`. Ten emit sites in all — 4 in Epic, 6 in Amazon. Validation changed the impact story:

   - **The audit's stated impact was half wrong.** It said "the game-info refresh silently no-ops". The store-shaped `DOWNLOAD_COMPLETE` does have no `game`, so `extractAppId` returns `null` and *that event's* `invalidateGameInfo` / `bumpGameStateVersion` / `invalidateGameSize` are skipped — but the worker's later event carries `game` and does the refresh. Nothing was lost; it cost one wasted `get_download_queue` round-trip.
   - **The real user-facing symptom was a duplicate failure toast, and this audit missed it.** `download-store.ts:onFailed` toasts on *every* `download_failed`. A failed Epic or Amazon install produced two: the store-shaped one first, with no game name (it had no `item.game_title`) and then the worker's, titled. Same body from `friendlyDownloadError`, 7.5s each. GOG, Ubisoft and Battle.net showed one.
   - **The store-shaped completion also fired early.** Epic emitted it in `_finalize_install`, *before* the worker ran prefix warmup — allowed 600s. Nothing consumed it that way, but that is the exact shape of a bug this repo has already had: `launcher/wrapper_prefix_probe.py:22` records Battle.net installs dying with `GameNotFoundError` because a "premature `DOWNLOAD_COMPLETE` wrote a `games.map` row".
   - **The counters that ship in support bundles read double for two of six stores.** `download_completed` / `download_failed` incremented twice per Epic/Amazon install (`rpc/mixins/observability.py:214` folds `plugin_metrics` into the bundle at `environment.json` → `frontend.plugin_metrics.counters`).
   - **`services/shortcut/events.py:_on_download_complete` was one guard away from acting on the bad payload.** Its `isinstance(game, Game)` check is the only reason a store-shaped completion didn't reach `mark_installed` with no game — the same silent-no-op class as item #1.

   Fix: deleted all ten store-installer emits (`_fail` in Epic is now a pure `InstallResult` builder), and unified the one remaining shape — `DownloadWorker._update_progress` now emits `item=item.to_dict()` instead of flat `store`/`game_id`/`progress`/`speed_mbps`/`eta_seconds`, so all five `DOWNLOAD_*` events carry the queue item. That let the frontend key progress by row id (`mergeProgressIntoSnapshot`) instead of writing every tick onto whatever row was on screen, and carries `download_phase`/`phase_message` on the tick rather than on the next refetch.

   Three defects found while tracing this one, all fixed here because they live in the same payload path:

   - **`download_duration_ms` measured the wrong window.** `_run_prefix_warmup` re-emits `DOWNLOAD_STARTED` to push the "preparing" phase to the frontend, and `_on_download_start` *assigned* the pending start time — so the recorded duration was the warmup span, not the install. True for GOG too, so not caused by the double emission, just hidden behind it. Now `setdefault`: the first start wins.
   - **A cancelled install leaked a `_pending_timers` entry for the plugin's lifetime.** Metrics subscribed to `DOWNLOAD_FAILED` but not `DOWNLOAD_CANCELLED`, while `test_failed_download_drops_the_pending_timer` called this out as "the leak that mattered" with the cancel half still open. These two are **one** fix, not two: `setdefault` is only safe because all three terminal events now clear the entry — otherwise a retry inherits the abandoned attempt's clock.
   - **The frontend wrote every progress tick onto `queue.current`** regardless of which game the event described. Dormant only because `DownloadService`'s `max_concurrent` defaults to 1. `mergeProgressIntoSnapshot` now matches on `item.id` and returns the previous snapshot object unchanged on a miss, so a non-matching tick costs no re-render either.

   Guard: `CANONICAL_SCHEMA` in `scripts/validate_event_schemas.py` is narrowed to the post-fix kwargs, and a new `EMITTER_OWNERS` check asserts `DOWNLOAD_*` is only emitted from `services/download/`. The narrowed schema catches a duplicate that invents its own payload; the ownership check catches one that copies the right payload from the wrong place. Both were verified to fail the gate against a deliberately-planted violation, not just to pass against the clean tree.

   **Lesson for the rest of this register:** the mechanism can be right while the named consequence is wrong. Two emitters were real, but the consequence the audit predicted (a lost refresh) was absorbed by the duplicate, and the consequence it didn't predict (a doubled toast) was the one users saw. Trace what each *consumer* does with each payload before writing down the impact.

### 1.2 Dead RPC methods (implemented, zero frontend callers)

~~Roughly 13 implemented methods have no caller.~~ **RESOLVED — and both of this section's own lists were undercounts of the same thing.** The two lists are disjoint: the 14 named here and the 15 the validator reported were different methods, and nobody added them up. The real figure was **29 of 102 mixin RPC methods, 28% of the surface.** Re-derived by cross-referencing public `async def` on `*Mixin` classes (the surface `@auto_wrap_rpc_methods` exposes) against every `.ts`/`.tsx` for both the snake_case name and `rpcRoutes.<camelCase>`; every remaining textual hit was read and all were comments, docstrings or one test fixture string. No dynamic route composition exists (`rpcRoutes[...]` indexing appears nowhere), so there was no hidden caller path.

Two structural causes, not 29 independent oversights:

- **A phantom UI.** "DiagnosticsPanel" is named in six backend docstrings (`probe_reaction_service.py:9`, `security/audit_log.py:8`, `security/config.py:72`, `security/auth.py:72`, `security/redaction.py:12`, `bootstrap/boot.py:153`) and has never existed in `src/`. Eleven RPCs were its backend half. `main.py:167` called the config-validation result "shown in the UI"; `boot.py:153` told the user they "can still see the DiagnosticsPanel and fix their config". Neither was true.
- **Superseded routes were never removed.** Six methods had been replaced by a better route — a bulk sweep, a display variant, the support bundle — and left declared in `rpc-routes.ts`.

**Why the guard missed 14 of them.** `find_dead_rpc` asked only "does this snake_case name appear anywhere in `src/` text". A method declared in `rpcRoutes` whose constant nothing references passes that test, because the route table itself keeps the name present. That is the whole gap, and closing it was the highest-value part of the fix.

**Three were dead wrappers over very live functions** — per §1.1.1's lesson, the wrapper went and the function stayed: `save_proton_setting` (live at `compatibility/proton_helpers.py:309`, called by `prefix_setup.py:183` and `ge_fallback.py:105`), `is_edge_installed` (live at `auth/edge_browser/detection.py:126`), `get_plugin_metrics` (live at `core/metrics_collector.py:120`, folded into the support bundle).

**Runtime proof before deletion.** A throwaway one-line probe in `rpc_wrapper` logged every RPC invocation — all 102, so it validated the analysis in both directions. Across a full exercise (boot in Desktop Mode, QAM, library sync, five App-Details opens, two game launches, updater modal, Capture Logs, store sign-out/in) **34 of the 73 live methods fired and none of the 29 suspects did.** The remaining 39 live methods sit on paths not exercised (installs, cloud-save push/pull, per-store auth contexts); `get_compat_tool_for_game` in particular is conditionally live on the Ubisoft/Battle.net launch path only. This step is what §1.1.1 says to do and it is cheap; it should precede any future deletion pass.

**What the user actually experienced.** For 26 of the 29, nothing — they were unreachable code, and deleting them changed no behaviour. Three were real:

- **Cloud-save failure toasts could not be silenced.** `cloud.failure_behavior.<store>` is read live at launch (`launcher/cloud/cloud_failure.py:133-141,161`) and defaults to `"toast"`, so a store whose cloud sync fails toasted on every launch of that game with no way to turn it off. The setting worked; only its UI was missing. **Decided: current behaviour is acceptable**, so the two RPCs were deleted rather than wired — the config key stays hand-editable.
- **A malformed user config was completely silent.** `validate_config_at_startup` set `_config_validation_result`, set `_config_degraded`, and emitted `CONFIG_VALIDATION_FAILED` — and all three landed in dead ends: the result was read only by the dead `get_config_validation_status`; `_config_degraded` was **written and never read anywhere**, so "degraded mode" had no behaviour at all; and the event fed SecurityService's audit log, readable only through five dead RPCs. Typo a key in `~/.config/unifideck/config.json` and you got one log line.
- **The circuit-breaker / launch-log / save-folder trio** was already register 4a/4b. Five of the 29 are their RPC halves, so they are resolved there, not twice.

**Cost to support rather than to users.** The support bundle is how reports actually arrive, and it carried no bus health, no security timeline and no config-validation result. Worse, `_support_bundle_extra` *promised* `runtime_probes` while delivering nothing (its only writer was the uncalled `report_runtime_probes`) and promised `plugin_metrics` while delivering empty timers/gauges (register #2).

Fix, in two passes. **Phase 1** deleted 12 superseded or unwanted routes, each against a verified live replacement: `get_sync_status` (an exact alias of `get_sync_progress` — both are `return self.sync_service.get_status()`), `check_game_update`, `get_game_metadata`, `get_release_notes`, `export_launch_logs`, `is_edge_installed`, `save_proton_setting`, `get_all_playtimes`, `sync_playtime_now`, `inject_hide_css`, and the two `cloud_failure` routes. `inject_hide_css` was the only reachable path into `cdp/cdp_inject.py`, so that module went too (166 lines; its `create_cef_debugging_flag` re-export had always resolved to `None` because `cdp_utils` has never existed on disk). `get_available_updates` was rerouted through `_request_update_scan`, which `check_game_update`'s deletion had orphaned, removing a duplicated sweep-refresh.

**Phase 2** took the DiagnosticsPanel cluster. Rather than delete the signal with the route, three snapshots moved into `_support_bundle_extra` — the delivery channel that demonstrably works — and then all 12 routes went: the five `security` methods, `get_bus_health` (body demoted to a private `_bus_health` collector), `get_probe_history`, `get_feature_flags`, `get_config_validation_status`, `get_plugin_metrics`, `release_quarantine`, `report_runtime_probes`. `SecurityRPCMixin`, `ConfigValidationRPCMixin` and `CloudFailureRPCMixin` emptied out entirely and were deleted; the mixin count went 20 → 17.

**The fold needed one real fix, not just a test.** `ValidationError.message` is `err.message[:256]` straight from jsonschema (`config/validator.py:423`), and jsonschema interpolates the offending *instance* — a bad `stores.*.client_id` renders as `"'sk-abc123' is not of type 'integer'"`. `scrub.py` is key-based and cannot catch a value embedded in a message string, so folding the raw error list into a bundle people paste in public would have leaked config values. Added a `safe_message` field built from `err.validator` + `err.validator_value` (both schema-side, e.g. `"type != integer"`); the bundle carries `{source, path, safe_message}` and `message` stays log-only. The security audit log needed no such work: `audit_emitter._emit` already runs `redact_for_audit` *before* `bus.emit` (`security/audit_emitter.py:92`), so `AuditLog.record` only ever stores redacted payloads — note the guarantee lives in the emitter, not in `record`, which stores `dict(payload)` raw.

**One planned fold was dropped as dishonest.** Probe history was going to be a fourth bundle key, but `report_runtime_probes` is the sole writer of `self.runtime_probes` and had no caller, so the key had never appeared in a bundle and folding it would have folded a structurally-always-empty value. Dropped instead, with a register row for the pipeline as a whole (below).

**The guard is now a hard gate** (`scripts/validate_architecture.py`). It asks both questions — undeclared, and declared-but-unreferenced — and excludes `rpc-routes.ts` from its own haystack so a route row can never vouch for itself. Opt-out is an inline `# no-frontend-caller: <reason>` marker at the definition, scanned through the whole contiguous comment block above the `def` so a real explanation can span lines. The marker is named for exactly what the check tests, so it stays honest whether the reason is "only the launcher calls this" or "dead, tracked by register 4a" — the five survivors carry the latter. The exemption count prints on every clean run, so the set cannot grow quietly, which is the failure mode of any allowlist.

**Two things the on-device validation did *not* cover, recorded so they are not mistaken for verified.** The `config_validation` block was exercised only on a *valid* config (`error_count: 0`), because the dev machine's config is well-formed; the populated-with-errors path — including the `safe_message` redaction that matters most — is covered by unit test (`test_support_bundle_rpc.py::test_config_validation_never_carries_the_offending_value` and `test_config_validator_safe_message.py`) rather than by a real bundle. And the `edge_not_installed` reactive path (§1.2's justification for deleting `is_edge_installed`) is untestable on a host with Edge present, which this one is; the path was verified intact in the shipped bundle and in all four browser-auth stores, not triggered.

### 1.3 Event-bus mismatches

~~Three dead handlers and seven unconsumed events.~~ **RESOLVED — one line of this section was wrong, one was stale, and the two highest-impact defects were not in it.** Re-derived by cross-referencing every `Events` member against emit sites, `@subscribe`, frontend subscriptions in both constant and string form, `src/types/events.ts`, and `WATCHED_EVENTS` — handling indirect emitters (`_emit_down`/`_emit_up`, `audit_emitter`, `wrapper_signals`) that a naive `Events.X` grep misses.

**Subscribed but never emitted (dead handlers):**

- ~~`SUSPEND`/`RESUME`~~ **RESOLVED — and this was the one item here that wrote bad data to a third party.** The finding held (subscribed at `playtime/service.py:274,286`, no emitter, and no suspend detection of any kind exists in the tree — no logind listener, no SteamClient hook), but it understated the consequence. `_provisional_duration` was `wall_clock − total_sleep_secs` and the sleep total was permanently `0`, so **suspending the Deck mid-game billed the entire suspend as playtime**: put a game down at 23:00 without quitting, wake at 08:00, get a 9-hour session. That value reached `play_sessions`, `game_stats.total_secs` and the streak columns — and `PlaytimeSyncService` **pushed it to GOG and Epic**, where it cannot be retracted.

  Fix: not an emitter. `_provisional_duration` now measures awake time off `time.monotonic()` — `CLOCK_MONOTONIC` does not advance across suspend on Linux — so sleep is excluded with no signal at all, including on a hard suspend that would fire none. `started_at` stays wall-clock because `_update_daily_stats` needs a real date for day attribution. Both enum members retired; `total_sleep_secs`/`suspended_at` deleted. `tests/unit/test_playtime_suspend_accounting.py` simulates the gap by advancing the wall clock without advancing the monotonic clock, which is exactly what a real suspend does to a live process.

- `ARTWORK_REQUEST`: confirmed — subscribed `artwork/event_handlers.py:133`, never emitted, ever. **Kept deliberately** as the entry point for a force-refetch trigger (the `force=True` arm exists for the account-switch case) and marked `# unwired:`. The two artwork paths that do run reach ArtworkService by other routes.

- ~~`SHORTCUT_CREATED`~~ **RESOLVED by making the handler live, not by deleting it.** The finding held, but "the auth-shortcut-artwork feature is dead" overstated it — per §1.1.1's lesson, most of that subscriber's job was already done elsewhere. The handler only acts on `is_auth=True`, and of the six stores: Ubisoft has its own live artwork path (`ubisoft/auth/context.py:40`), and Epic/GOG/Amazon/Microsoft were migrated to ephemeral 15-second shortcuts (`_self_heal_auth_shortcuts` deletes their old persistent rows). The single real gap was **Battle.net**, the one store that both reaches `shared/auth_shortcut.py` and keeps a persistent tile — so its "Battle.net" sign-in tile rendered bare in the library.

  Fix: `ensure_auth_shortcut` now emits on its create branch only (not the already-in-VDF branch, which would re-run the SGDB lookup on every sign-in). The subscriber, the `_AUTH_TITLE_FOR_LOOKUP` mapping and the `CANONICAL_SCHEMA` contract already existed and already agreed — only the emit was missing. Guarded by driving the **real** `ArtworkService._on_shortcut_created` with whatever the emitter actually sent and asserting on the SGDB call, rather than against a hand-written payload: §1.1.1 shipped twice because both guards there pinned opposite halves of a mismatch.

**Emitted but never consumed:**

- ~~`TOAST_NOTIFICATION`~~ **RESOLVED (item #2)** — retired; its three emitters now use `LAUNCHER_STAGE`.
- `CIRCUIT_STATE_CHANGED`: confirmed no subscriber — **but this section missed the part that matters.** The enum docstring documented a payload that has never existed: it claimed `game_key`, `state`, `recent_count`, `failure_kinds`, while the emit site (`launch_history/service.py:110`) sends `store`, `game_id`, `is_open`, `failure_count`. `CANONICAL_SCHEMA` carried the real set, so the gate stayed green while the prose lied unchecked. The trigger values were wrong too (really `record_failure_<kind>`, `clear_failures`, `arm_bypass`, `closed`). Register **4a** exists to build the `useCircuitState` hook from that docstring and would have read `undefined` for four of five keys — the `app_id`/`game_id` failure of §1.1.1, repeated inside the fix for §1.1.2's silent failure. Docstring corrected against the emit site; the missing subscriber stays with 4a, now marked `# unwired:`.
- ~~`STORE_REGISTERED`~~ **This finding was wrong.** There *is* a live consumer: `src/stores/store-info-store.ts:46` subscribes and refetches store infos, and the boot-time emit reaches it through the replay buffer on the first poll. Only the enum docstring's "Consumed by metrics_collector.py" was false. Docstring corrected; no code change.
- ~~`STORE_ERROR`~~ **RESOLVED — retired, and it was worse than recorded.** Not merely unemitted today: `git log -S store_error -- py_modules/` reaches the initial commit and finds no emitter ever. The emitting half was never built while the frontend half sat there looking wired. Verified before deleting that every failure it could carry is already covered — a per-store sync failure logs, emits `SYNC_FAILED` and toasts via `LAUNCHER_STAGE` (`sync_run_mixin.py:389`); an auth failure logs, emits `STORE_AUTH_FAILED` and flips the store's frontend status (`store_registry.py:340`); and the `StoreError` *exception* is raised only inside `StoreBase._run_cli`, which §3.2 confirms is dead. Enum member, `events.ts` row, `WATCHED_EVENTS` row, frontend handler and the `toasts.storeError` string in all 16 locales all deleted.
- `PLAYTIME_SYNC_COMPLETE`: confirmed — emitted `playtime_sync/service.py:197`, no consumer, and `_emit_result`'s own docstring claimed "toast bridge consumes it", which was never true. `PLAYTIME_SYNC_FAILED` is worse and was not recorded: declared, carrying a `CANONICAL_SCHEMA` row, and **neither emitted nor consumed**. **Both kept deliberately** — surfacing "your playtime reached GOG" is wanted, it just has no UI yet — and marked `# unwired:` with the note that wiring it means a subscriber *plus* rows in `events.ts` *and* `WATCHED_EVENTS`, all three or it is silent again.
- ~~`SUBSCRIPTION_DETECTED`/`_EXPIRED`/`_CHECK_FAILED`~~ **RESOLVED — retired.** They were missing **three** legs, not one: no Python subscriber, absent from `src/types/events.ts`, and absent from `WATCHED_EVENTS`, so the frontend never even polled for them. The enum docstring claimed "Subscribers are the frontend toast listener and MetricsCollector"; neither existed. `SYNC_SKIPPED` (below) already carries the same news on a channel that is now rendered, so removing the emits also let `_store_tier_result` stop being a coroutine. The orphaned `microsoft.subscriptionDetected` string was removed from all 16 locales.
- `CONFIG_VALIDATION_FAILED`: **this line is stale.** `_config_degraded` is no longer write-only — the §1.2 pass reads it at `rpc/mixins/observability.py:250` and folds it into the support bundle. Still true that it drives **no behaviour**. Two docstrings were still promising UI that does not exist (`main.py:164` "a boolean flag the frontend reads to display a non-blocking warning"; `boot.py:153` "the user can still see the DiagnosticsPanel"); both corrected.

**Found while resolving this section, not in the original register:**

- **Xbox Game Pass sync failures were completely silent** *(highest impact in §1.3)*. `MicrosoftStore` emits `SYNC_SKIPPED` with `reason` ∈ `{no_active_subscription, subscription_tier_unknown, subscription_check_error}` (`microsoft_store.py:349,360,372`). The event was declared in `events.ts`, polled via `WATCHED_EVENTS`, listed in `STALE_ON_RELOAD_EVENTS` — and had **no subscriber anywhere**. So any transient probe failure (network blip, expired token, GSSV chain failure) dropped the entire xCloud library from the sync while the bar reported success for the other five stores: it reads as "my Game Pass games vanished". The three explanatory toast strings had been written and translated into all 16 locales the whole time and were referenced by nothing in `src/` — the exact shape of §1.1.2, where "the i18n strings existed and were translated in all 16 locales; only the delivery channel was dead." Fixed with one subscriber in `boot-event-listener.tsx` mapping `reason` → the existing keys, keyed by reason rather than by store and silent on unknown reasons so a future subscription store adds one row. Zero new strings, zero locale churn. `tests/unit/test_sync_skipped_toast_wiring.py` pins all three links of the chain, including that every mapped key is translated in every locale.
- **`PLUGIN_LOADED` / `PLUGIN_UNLOADING` were dead in both directions** *(found by the new gate, below)*. Never emitted, never subscribed; they only looked load-bearing because `event_priority.py` assigned them `EventPriority.CRITICAL`. Plugin lifecycle is not event-driven here and never has been — boot runs through `bootstrap/boot.py`'s explicit layer calls and shutdown through `teardown.py`'s ordered list, both with ordering guarantees a bus fan-out could not give them. Retired.
- **`ACCOUNT_SWITCHED` documented a `previous_user_id` key that is never sent.** Benign only because `UserPathsCoordinator` reads `active_user_id` with a `new_user` fallback. Docstring corrected.
- **Nothing could catch this defect class.** `validate_event_schemas.py` is emit-side only — kwargs contract plus emitter ownership — with no "declared but never emitted" or "subscribed but never emitted" check, and nothing checked the three-place mirror §1.4 flags. Three dead rows were already sitting in `CANONICAL_SCHEMA` (`SHORTCUT_CREATED`, `PLAYTIME_SYNC_FAILED`, `RUNTIME_PROBES_REPORTED`), each with a full kwargs contract and zero emitters, which is precisely what made them read as wired. New hard gate `scripts/validate_event_wiring.py` requires every enum member to have a producer *and* a consumer, treats the frontend leg as complete only when all three of `events.ts` / `WATCHED_EVENTS` / an actual subscription are present, and reports **which** leg is missing since each fails differently. Opt-out is an inline `# unwired: <reason>` marker at the member, scanned through the whole comment block, with the exemption count printed on every clean run (§1.2's pattern). AST-based, because the dominant emit style in this tree puts the event name on the next line and a line-oriented gate would produce dozens of false positives — which trains people to ignore it. Its two blind spots (`INDIRECT_EMITTERS`, `INDIRECT_FRONTEND_SUBSCRIBERS`) are explicit tables that each name the file doing the work, and a test pins them to the enum so a retired event cannot leave a hole. Verified against four planted violations, not just against a clean tree (§1.1.4's lesson): a ghost member, and `SYNC_SKIPPED` with each of its three frontend legs removed in turn.

  **Lesson for the rest of this register:** a section of an audit is itself a claim to be validated. Of eleven lines here, one was outright wrong (`STORE_REGISTERED` has a live consumer), one was stale, one understated its impact by an order of magnitude, one overstated it, and the two costliest defects were absent. Re-derive from the tree before working a section; do not work the list.

### 1.4 Redundancy outside stores

- ~~**Two `config.vdf` CompatToolMapping injectors**~~ **RESOLVED (item #1).** `services/proton_service.py`'s `_inject_compat_tool` (weaker regex, unreachable) was deleted along with the rest of that service's compat-tool path. `compatibility/proton_helpers.py:65` (`ProtonToolsManager`) is now the only writer.
**RESOLVED — and this section held up worse than any other. Of its eight open lines, two were outright wrong, one undercounted by more than 4×, and the largest item was filed as a tidy-up while concealing a live credential exposure.** Every line was re-derived from the tree before being worked, per §1.3's lesson. Verdicts below in the original order.

- ~~**`resolve_proton_path` name collision**~~ **RESOLVED — reclassified, then deleted.** The claim of "two public functions, same name, incompatible semantics" was true but described the wrong hazard. `compatibility/proton_helpers.py:330` was `return tool_name` — an identity stub with **zero callers**, reachable only because `compatibility/__init__.py` re-exported it in `__all__`. It was not a live collision anyone could trip over at runtime; it was a trap for the next importer, and a nastier one than the audit implied. `from unifideck.compatibility import resolve_proton_path` looks entirely legitimate, and would have returned a tool *name* where a *path* was expected — not a crash, a silent mis-resolve, the same failure class as §1.1.1's `app_id`/`game_id`. Deleted along with its two export lines; `launcher/proton/infrastructure/selector.py:158` is now the only `resolve_proton_path`.

- ~~**Duplicated encrypted-token persistence**~~ **RESOLVED — extracted, and two latent bugs came out with it.** The finding held exactly. Extracted the shared core into `security/token_file.py` (`EncryptedTokenFile`: read, decrypt, legacy-plaintext detect, atomic 0600 write, permission audit). Each store keeps only what is genuinely its own — GOG the `GOGUserInfo` shaping and its gogdl plaintext mirror, Microsoft the legacy path resolution and `scope`.

  Placed in `security/` rather than the `shared/` the register named: the module has no store-specific content (its dependencies are `SecureTokenStore`, `secure_io` and the audit emitters, all in that package), and `stores/` sits *above* `security/`, so putting it under `stores/shared/` would invert the layering. The store name it takes is an audit label, not a switch. The store-*facing* helper added for the item below correctly does live in `stores/shared/`.

  Two defects found only because the copies were being read side by side:
  - **Microsoft's `_write_atomic_0600` double-closed the fd.** `os.fdopen` takes ownership, so when the *write* failed the `with` closed it and the `except` arm then called `os.close(fd)` again, raising `EBADF` **over the real error**. A disk-full token save was logged as "Bad file descriptor" — a support bundle reporting the wrong cause for a credential write failure. GOG's copy was correct; the shared version keeps that one. Pinned by a test verified against the planted original, not just against the clean tree (§1.1.4's lesson): it reproduces `[Errno 9] Bad file descriptor` and the fixed version reports `ENOSPC`.
  - GOG emitted `permissions_check` with no `bus is not None` guard where Microsoft had one. Kept the guard.

- ~~**Duplicated `find_umu_run`**~~ **This finding was WRONG.** `stores/shared/wine_env.py:52` is not a second implementation; it is a four-line *delegation* to `UbisoftBinaryResolver.find_umu_run()`, and `wine_env.py`'s module docstring says so in as many words: "Delegates to Ubisoft's resolver, which already solves all of this, rather than reimplementing it." One implementation exists. §3.3 of this same audit states the delegation correctly — the two sections disagreed and nobody reconciled them. No code change. The real (deliberate, accepted, already-commented) observation available here is a layering one: `shared/` reaches into a store package, lazily and inside a method so the coupling reads as incidental rather than structural.

- ~~**Three appid-to-u32 conversions**~~ **RESOLVED — there were thirteen, across two different conversions.** The count was low by more than 4×, and the three sites it named included one that must *not* be folded in. The first grep missed most of them because roughly a third of the tree spells `2**32` as `0x100000000`.

  **Signed→unsigned (12 sites, now 1):** `core/compat_bridge.to_unsigned` was already the canonical helper with one external consumer (`compatdata_scan.py`). Routed onto it: `orphan_scan.py`, `registry.py`, `shared/auth_shortcut.py` (×2), `ubisoft/auth/shortcut.py`, `selector.py`, `rpc/mixins/auth_shortcuts.py` (×2), `services/artwork/fetcher.py` (×3), `rpc/mixins/sync_cleanup.py`. The launcher site was safe to include because `compat_bridge.py` already holds the 3.10–3.14 system-Python constraint and `launcher/proton/prefix_setup.py` already imports from it.

  **NOT the same conversion, deliberately left alone and now commented:** `services/shortcut/events.py:22` is `(appid & 0xFFFFFFFF) | 0x80000000`, which additionally *forces* the high bit — the invariant `games_map.py:82` establishes when it generates an appid (`crc32(key) | 0x80000000`) and the form Steam names grid files with. Folding it into `to_unsigned` would have been a silent behaviour change. Also left alone: the three `_appid_key_candidates` helpers (`compatibility/library.py`, `core/sync_queries_mixin.py`, `rpc/mixins/_metadata_display.py`), which generate *both* forms for a lookup rather than converting. They are triplicated, but that is §3.4-class cross-copy redundancy, not this one.

  **Zero live defect**: all twelve agreed for any valid int32. The gain is that `to_unsigned` does `int(app_id)`, so the sites now tolerate the string form appids take across the RPC boundary, where the inline `appid >= 0` comparisons would have raised `TypeError`.

- ~~**Security package split with a circular import**~~ **RESOLVED — and this was the largest thing in §1.4, filed as the smallest.** The cycle was real: `ephemeral_creds.py` (440 lines) carried a bottom-of-file re-export from `ephemeral_creds_inplace.py` (310 lines), which imports back from it, with a comment recording that the re-export had once been documented but not written and raised `ImportError` at module load. What the finding missed is that **both modules were 100% dead** — `EphemeralCredentialContext`, `InPlaceEphemeralFile` and `EphemeralCredentialError` had zero call sites anywhere: backend, launcher, `bin/`, `src/`, tests, `vulture_whitelist.py`. It was a cycle between two modules nothing imported.

  **They were built to close a gap that is still open, and that is the actual finding.** Epic's and Amazon's credentials are owned by legendary and nile, which write `~/.config/{legendary,nile}/user.json` at **0644 with a live access token and refresh token in plaintext**, while GOG and Microsoft go through `SecureTokenStore` at 0600. Verified on the dev Deck: `-rw-r--r-- 6238 user.json` containing `{"access_token": "eg1~…"}`, and a `user.json.corrupt-1785320102` quarantine copy from `nile_lock.py` sitting at the same mode with month-old credentials still in it. Meanwhile `legendary.py:48` documented `security.ephemeral_creds` as pointing `LEGENDARY_CONFIG_DIR` at an isolated directory — nothing ever did. The same phantom-mechanism shape as §1.2's DiagnosticsPanel: a fully-written, fully-documented feature whose caller was never built, reading as live to anyone who greps for it.

  On a Deck that file is readable by every other local account **and by every game the user launches**, because Proton maps `$HOME` into the prefix.

  Fix: deleted both modules (750 lines) and their `security/__init__.py` exports, corrected the `legendary.py` docstring, and closed the exposure with mode rather than encryption — new `stores/shared/cli_credentials.harden_cli_credential_file`, which chmods 0600 and emits the existing `emit_permissions_check` so all four token-holding stores now report through one audit channel into the support bundle's `security` block. **Stated plainly so it is not oversold: this is not encryption at rest.** Anything running as the user can still read these files. Full ephemeral wrapping was considered and deferred — it touches every legendary/nile invocation on the two most-used stores.

  It hooks `_check_*_authenticated` rather than `complete_auth` deliberately: both CLIs rewrite `user.json` at 0644 on **every token refresh**, so a one-shot chmod at sign-in silently drifts back. The auth check runs on every store-status refresh and sync, costs one `stat` when nothing has changed, and is the only path both stores share (§3.2: `StoreBase._run_cli` is dead, so there is no subprocess choke point). A test pins exactly that rotation case.

  `device_fingerprint.py` vs `device_identity.py` **is not an overlap** — the second half of this line was also wrong. `DeviceFingerprint` *composes* `DeviceIdentity` (constructor parameter), and both are live: `DeviceIdentity` derives the encryption key at `secure_token_store.py:273`, `DeviceFingerprint` backs `services/security/device_reset.check_device_fingerprint`. That is layering, not duplication. No code change.

- ~~**Write-only install manifest**~~ **This finding was WRONG, and acting on it would have caused silent data loss.** `.unifideck_manifest.json` is not written-and-never-read. The reader is `core/marker_sweep.py` (`_MANIFEST_MARKER`, line 40), live from `rpc/mixins/sync_cleanup.py:328,346` and `rpc/mixins/download.py:264`: `iter_marked_dirs` parses `store` and `store_id` back out, and that is the **proof Unifideck created a directory** which `find_for_game` / `sweep_game` / `sweep_all` require before deleting anything. It is the only mechanism that can clean up a game installed outside the default library root — a custom folder or an SD card, which the store CLIs do not scan. Dropping the write, as the audit invited, would have stranded every such Epic/Amazon install on disk **while uninstall reported success** — precisely the bug `marker_sweep`'s own docstring says it exists to fix.

  The line was reached honestly: `core/manifest.py` genuinely has no reader, because the reader lives in a different module and the two never referenced each other. That absence is the real defect, so the fix is documentation — `manifest.py`'s docstring now names `marker_sweep` and what depends on it, and `marker_sweep.py` notes that it is the sole reader and that a search in `manifest.py` will turn up nothing. Neither can now be mistaken for dead.

  **Lesson: "no reader in this module" is not "no reader."** Before deleting a *write*, grep for the filename or constant it produces, not just for the function that consumes it.

- **`steam/current_user.localconfig_path` has no production caller** *(unchanged)*: confirmed still true and still deliberate — a correct, exported, unit-tested path helper symmetric with `shortcuts_path` / `grid_dir`, in a module whose job is per-user Steam paths. Noted so it is not mistaken for live wiring.

- ~~**Frontend event names maintained in three places**~~ **The observation stands; the risk it describes is already closed.** All three places still exist (`core/types/events.py`, `src/types/events.ts`, `src/api/event-bus-client.ts`'s `WATCHED_EVENTS`), but "an omission slips through" no longer holds: `scripts/validate_event_wiring.py`, built during the §1.3 pass, treats the frontend leg as complete only when all three are present, names which leg is missing, and is a hard gate in CI (`.github/workflows/tests.yml:74`). What remains is ergonomic — three hand-edits per event — not a correctness gap. Deliberate omissions such as `LIBRARY_SYNC_*` are carried as explicit `# unwired:` exemptions with the count printed on every clean run. No code change.

---

## Part 2 — Intended design vs current state

### 2.1 Mixin count: four contradictory numbers

| Where | Claimed | Actual at audit time |
|---|---|---|
| `main.py:17` docstring | "eleven mixins" | 20 |
| `rpc/mixins/__init__.py` `__all__` | 13 | 20 (missing Account, Achievements, AuthShortcuts, Edge, Executable, LibraryFacets, Storage) |
| `docs/architecture.md:127` | 18 | 20 |
| `SKILL.md:19` / `backend.md:15` | 20 | correct |

Two more (`CleanupRPCMixin`, `_CleanupFinalizeMixin`) ride in transitively through `SyncRPCMixin`'s inheritance and are counted nowhere.

**RESOLVED 2026-08-25 — but the first resolution was half-applied, and the half that was applied by correcting a number was wrong again before this document was finished.**

`main.py` and `__all__` were fixed and their agreement machine-enforced (`validate_architecture.py` check 1), which is the only invariant that matters. That half held: it survived the §1.2 pass emptying and deleting `SecurityRPCMixin`, `ConfigValidationRPCMixin` and `CloudFailureRPCMixin` (20 → 17), and the check caught the drift in the same change. `main.py`'s docstring points at the check instead of naming a figure.

The other half was never done. The recorded fix says "stop restating the number in prose rather than correct it in five places", but only `main.py` was de-numbered; the remaining sites were hand-corrected to **20** and went stale in the same release the §1.2 deletions landed. Re-derived against the current tree, the table above reads:

| Site | Audit said | Said at re-validation | Actual | |
|---|---|---|---|---|
| `main.py` docstring | "eleven mixins" | no numeral, points at the check | 17 | held |
| `rpc/mixins/__init__.py` `__all__` | 13 | 17, matches `main.py` | 17 | held, machine-enforced |
| `docs/architecture.md:133` | 18 | **20** | 17 | still wrong, new wrong value |
| `SKILL.md:19` / `backend.md:15` | 20 *(correct then)* | **20** | 17 | went stale underneath |

Three more sites this section's table never listed: `docs/architecture.md:230` ("the 20 mixin surfaces"), `docs/engineering-roadmap.md:66` (present tense, both halves false, and it is the evidence for roadmap item 8 *Stale-docs guard*), and `validate_architecture.py`'s own docstring, asserting in the present tense that `main.py` composes 20 inside the file whose job is catching that drift.

Two things found while re-validating that changed the fix:

- **The Layer-6 table at `architecture.md:137-153` was already complete and correct** — 17 rows matching `main.py` exactly. The doc enumerated the right set three lines below a sentence claiming a different number. The correct answer was on the page the whole time, which is why de-numbering the sentence costs nothing: the table becomes the page's single enumeration.
- **`.github/workflows/tests.yml:78` understated its own gate**, calling the dead-RPC check "report-only" when §1.2 had made it hard (`hard_failures += len(dead)`). A CI comment that calls a hard gate advisory is how a red build gets waved through. Corrected.

Re-verified as still accurate and needing no change: the transitive pair (`SyncRPCMixin` → `CleanupRPCMixin` → `_CleanupFinalizeMixin`) is correctly absent from `__all__`, whose contract is "every mixin composed in `main.py`". Their RPC surface is not unguarded either: `collect_rpc_methods` globs the whole directory and matches `name.endswith("Mixin")`, so `scan_orphaned_shortcuts` and `perform_full_cleanup` are covered by check 4.

Fix: removed the numeral from all five live sites so the set is stated in exactly two machine-checked places, then added **check 5** to `validate_architecture.py` so a third statement cannot appear. It scans `CLAUDE.md`, `docs/`, `.claude/skills/`, `main.py`, `py_modules/`, `scripts/` and the workflows, excludes `docs/archive/` and this file (both exist to hold the historical figures), and opts out via an inline `mixin-count-ok: <reason>` marker in the house style of `# no-frontend-caller:` and `# unwired:`. Used once, on the roadmap evidence line. New `tests/unit/test_validate_architecture.py` (26 tests) pins both checks against planted violations in both directions, and was itself mutation-tested: removing the lookbehind, shortening the marker's reach, and truncating the line scan each fail the specific test written for them.

The check deliberately matches only a count written *next to* the word, which is the form all seven instances took. A figure separated from the word by other prose is not caught; widening the window costs more in false positives than it buys. One false positive was found and fixed during implementation: `Layer-6 RPC mixins` in `services/__init__.py:7` read as a count of six, because a word boundary sits between the hyphen and the digit. A gate that fires on untouched code gets switched off rather than fixed, so the lookbehind is load-bearing and has its own test.

**Lesson for the rest of this register: correcting a number is not fixing a drift, it is re-arming it.** This count was corrected twice and was wrong both times within a release. The durable fix is to delete every copy but the one a machine checks. Before ticking a P2 documentation item, check whether the fix removes the duplicated fact or just refreshes it.

### 2.2 Four incompatible layer maps

No single agreed layer model exists:

1. `py_modules/unifideck/__init__.py` — "5 layers", no RPC layer, L3 named `stores/store_base` (the file is actually `stores/shared/store_base.py`).
2. `docs/architecture.md:39-128` — titled "5-Layer" but draws **six** (L6 RPC → L1 Types).
3. `main.py:20-24` — says "five-layer" but lists **six**, with L3 = "event bus/cache/config" and L1 = "paths/I/O".
4. `CLAUDE.md:17` — non-numbered: `rpc/ (leaf) → services/ → stores/ + core/ + event_bus/`.

Direct contradictions: "five" vs six enumerated layers; Layer 1 is `core/types` vs `paths/I/O`; Layer 3 is `store_base` vs `stores/shared` vs `event_bus/cache/config`.

**RESOLVED 2026-08-25 — and the first resolution missed more sites than it fixed.** Register item 13 scoped itself to `docs/architecture.md`, so the four sites above were cleaned and the drift was declared closed. Re-derived against the current tree, five restatements were still live, none of them in the file item 13 named:

| Site | Said | |
|---|---|---|
| `README.md:94` | "The 5-layer backend" | the public face of the repo |
| `README.md:175` | "a 5-layer architecture" | |
| `event_bus/__init__.py:16` | "Layer 3 of the plan's five-layer model" | cites a "plan" no living doc contains |
| `.github/workflows/complexity.yml:4` | "the five-layer architecture of the technical document v1.1" | **found by the new check, not by this audit** |
| `.github/workflows/quality.yml:96` | "The full 5-layer stack" | **found by the new check, not by this audit** |

Plus `.importlinter:3` ("The full 5-layer stack doesn't cleanly map…"), whose sentence exists to explain why the contract is narrow and needs no figure at all.

Fix: removed the numeral from all six, and added **check 6** to `validate_architecture.py` so a seventh cannot appear. The two workflow comments are the point: this section listed four sites, the fix cleaned four sites, and a machine check immediately found two more. Per §2.1's lesson, the check is the fix and the edits are incidental.

**The check's own false-positive class is worth recording.** A first cut banned any cardinal before "layer" and fired on seven correct lines: `config/` legitimately describes a "3-layer merge" (defaults, user, code), and `config_manager.py` a "3-layer configuration manager". Neither is about the architecture stack. Requiring an architecture noun after the word (`backend|architecture|stack|model|design`) separates them without seven opt-out markers on untouched code. Ordinal use (`Layer 3`, `Layer-6`) is excluded for free, because those put the number after the word. Both classes are parametrised with verbatim lines in `tests/unit/test_validate_architecture.py`.

### 2.3 StoreBase contract is documented wrong

`docs/architecture.md:90-91` says StoreBase "defines the five abstract methods … `get_library()`, `install()`, `uninstall()`, `launch()`, `get_updates()`." The real `store_base.py:51-97` defines **10** abstract methods (`is_available`, `start_auth`, `complete_auth`, `logout`, `get_library`, `install_game`, `uninstall_game`, `update_game`, `check_for_updates`, `get_game_size`) and has **no `launch()`** (launch lives in `launcher/dispatcher.py`, not the store contract). None of the four documented method names matches reality.

**RESOLVED, re-verified 2026-08-25.** `architecture.md:92` now says "the ten abstract methods" and lists exactly the ten the ABC declares, in order, with an explicit note that launching is owned by `launcher/dispatcher.py`. Checked name by name against the tree; no drift. This is the one Part 2 item that needed nothing.

### 2.4 Battle.net is a sixth store the docs deny

`docs/architecture.md` ("five store connectors"), `stores.md`, `launcher.md`, `CLAUDE.md`, and multiple in-code "five stores" comments all describe a five-store system. The code has six: `stores/battlenet/` is a full connector (store, library, install, agent_status, id_map, ownership, product_db, prefix), plus 8 `launcher/proton/handlers/battlenet*.py` files, and `bootstrap/cache_registry.py:18` already says "Six stores". The only Battle.net doc is `docs/feasibility/battlenet.md`, still framed as a feasibility study of an unimplemented store.

**RESOLVED 2026-08-25.** Two of the register's open notes were already stale: `docs/feasibility/battlenet.md` opens with "**Status: implemented.** Battle.net shipped as the sixth store", and `architecture.md` says six in both its diagram and its prose. Two live residuals remained:

- `docs/engineering-roadmap.md:79` — "The five store connectors implement the same contract". This is the *evidence line* for a roadmap item, so it understated that item's own scope by a store.
- `CLAUDE.md:3` — the store list itself read "Epic, GOG, Amazon, Ubisoft, and Xbox Cloud Gaming", omitting Battle.net. A missing list entry, not a wrong count, so no count check can catch it.

Fix: corrected both, and added **check 7**, which *verifies* the count against the store directories on disk rather than banning it.

**Verifying rather than banning was forced by the data, and it is the opposite of checks 5 and 6.** A ban would red about ten live sites that state the count correctly while explaining something ("the sole dispatcher for all six stores' `DOWNLOAD_*` events"). Worse, a first cut matching any cardinal before "store" reported **23 false positives in one run**, every one a correct subset statement: "Amazon is the one store whose sign-in leaves the shared Edge profile", "four stores report credential permissions through one channel", "Two stores need this path". A count *below* the total is nearly always naming a subset. So check 7 examines only total-claim forms (`all N stores`, `N store connectors`, `N-store system`) and compares them against the tree. With that narrowing the whole repo needs no opt-out marker, including the drift-guard skill lines that quote "five stores" verbatim.

**Lesson: a count that is often correct needs a verifier; a count that should not exist needs a ban.** Reaching for the ban first would have produced a gate someone switched off within a release.

### 2.5 Undocumented subpackages and self-contradictions

- **`accounts/` is absent from every doc and skill** but fully implemented (`account_manager.py`, `AccountRPCMixin`).
- **`accounts/__init__.py` contradicts `accounts/account.py`**: the `__init__` docstring says `check_account_switch`/`migrate_account_data` "were never carried over"; `account.py` says it "Restores" them. They exist.
- **`services/` table is ~50% of reality**: `architecture.md` lists 13 service packages; `services/bootstrap/service_defs.py` registers 21 plus `LauncherService`. Missing from docs: `compatibility`, `activity_log`, `feature_flags`, `probe_reaction`, `launch_logs`, `support_bundle`, `achievements`, `playtime_sync`, `metrics`.
- **`core/` has ~30 modules vs ~11 documented**; notably six `sync_*_mixin.py` modules moved RPC-mixin logic *into* `core/`, blurring the documented "rpc is thin" boundary.
- **`event_bus/` has five undocumented modules** (`event_bus_devex`, `event_bus_extensions`, `event_bus_reliability`, `event_bus_scaling`, `event_priority`); `event_priority.py` vs the documented `priority_dispatcher.py` suggests two priority mechanisms.

**RESOLVED 2026-08-25. One bullet named a file that does not exist, and the counts were wrong in both directions.**

- **The `accounts/` bullet was two bullets, and only the harmless one was fixed.** `accounts/` is in the layer map now (`architecture.md:163`). But the docstring bullet blamed `accounts/account.py`, and there is no such file. The real contradiction was between `accounts/__init__.py` and `rpc/mixins/account.py:50`, and it survived: the package docstring still told every reader that `check_account_switch` / `migrate_account_data` "were never carried over, so the modal never appeared". All false. Both RPCs exist, `rpc-routes.ts:102-103` routes them, `bootstrap-tasks.tsx:335` calls them at boot, and `AccountSwitchModal.tsx` renders the result. **This was the highest-value item in Part 2** and the cheapest: a reader acting on it would delete a live feature whose job is stopping one Steam account's library and saves being served to another, and a §1.2-style dead-RPC pass would have flagged both RPCs as suspects on its authority. Rewritten to point at the mixin, with an explicit line telling the next reader not to trust an older copy of itself.
- **Re-derived counts.** Services: 10 `services/*` packages documented against 15 on disk (the audit said 13 against 21, counting registered DI keys rather than packages, which is why its missing-list named things like `metrics` and `db_path` that are keys, not packages). The genuinely absent packages were `achievements`, `compatibility`, `playtime_sync`, `support_bundle` and `updater`. `core/`: 6 documented against 20 on disk, not "~11 vs ~30" (`manifest.py` shrank when §1.1.1 deleted its discovery half). `event_bus/`: the five named were right.
- Worth naming what was invisible: `compatibility` is the ProtonDB and Deck-Verified path, and `support_bundle` is Capture Logs. Both read as nonexistent to anyone planning a change, and Capture Logs is the first thing to reach for on a bug report.

Fix: added the missing rows, split the sync orchestrator and its mixins into their own table (they are the "rpc is thin" blur this section flagged, and naming them is better than implying they belong in `rpc/`), and gave `event_bus/` a real table with an explicit **"On the emit path?"** column. Three modules are marked **no**, citing register items 4e and 4g, so the doc records that the priority/coalescing layer and the watchdog are unreachable rather than describing them as working. Documenting them as functional is how the layer stayed hidden for a release.

Then added **check 8**, which asserts every `services/*/` package and every `core/` and `event_bus/` module appears in `architecture.md`. A hand-maintained table drifts exactly the way a hand-maintained count does, so membership is checked rather than trusted. It caught 25 omissions on its first run.

### 2.6 Version drift

- `py_modules/unifideck/__init__.py:22` = `0.7.1`.
- `docs/architecture.md` and `CLAUDE.md` = `v0.7.0`.
- `package.json` and `plugin.json` = `0.7.4`.
- Current branch = `0.7.5`.
- Every skill file says "Last verified 2026-07-02 against v0.7.0" and has not been re-verified against the current tree.

**RESOLVED 2026-08-25, by deleting rather than bumping.** `__version__` had **zero readers** anywhere: not in `py_modules/unifideck`, `main.py`, `scripts`, `tests` or `bin`, and not via `getattr` or `importlib.metadata` either. Meanwhile `package.json` already had three: the updater (`services/updater/service.py:139`), the support bundle (which labels it "Installed plugin version (source of truth)", `support_bundle/sources.py:268`), and `build-plugin.sh:137`. So the drift was between one file everything read and one file nothing read. Bumping it would have re-armed exactly what §2.1 warns about; it is gone, with a docstring where it sat explaining why not to re-add it, and the `unifideck-drift-guard` lockstep row now says so too.

`CLAUDE.md` was self-contradicting on adjacent lines: the H1 said `(v0.7.0)` while line 3 said "Version source of truth: `package.json`". The numeral is gone from the H1.

**The `Last verified:` stamps were deliberately left alone.** A stamp reading "2026-07-02 against v0.7.0" is an accurate freshness signal, not drift. Bumping it without re-reading that file forges the signal, which is the precise failure roadmap item 8 exists to prevent. Re-verifying the six stale skill files is real work and is tracked separately.

### 2.7 Machine enforcement is narrower than claimed

`docs/architecture.md:41-42` claims the stack is "enforced by `.importlinter`." The `.importlinter` file itself admits "The full 5-layer stack doesn't cleanly map to the current tree" and enforces only two invariants (`rpc-is-leaf`, `types-is-leaf`), which §9 of the same doc correctly states. §3 and §9 of `architecture.md` disagree with each other.

**RESOLVED, re-verified 2026-08-25.** `architecture.md:44` now reads "The machine-enforced invariants are in §9 (`.importlinter`)", so §3 defers to §9 instead of contradicting it, and `.importlinter` still declares exactly the two contracts §9 documents (both verified KEPT by a `lint-imports` run). Never had a register line of its own; closed under item 13.

### 2.8 Dead code / phantom design vocabulary

- `launcher/types/options.py:71` `parse_launch_options` — dead, imported nowhere (correctly flagged in `launcher.md`).
- `rpc/mixins/__init__.py`, `store.py`, and `auto_wire.py` docstrings describe a "handler group" / `composer.bind_handlers` / `handlers/` design that does not exist. `handlers/` has no directory under `rpc/`. The guidance "add new RPC methods to a handler group" points at a structure that was never built. (Fixed in `mixins/__init__.py` as part of this audit; `store.py` and `auto_wire.py` remain.)
- `OP-24c` / `OP-25g` / `OP-26k` plan-reference markers threaded through mixin docstrings reference an operational plan numbering absent from living docs.
- `store.py:182` `inject_game_to_appinfo` is an explicit no-op stub returning `success=True`.
- `cleanup_sweeps.py` in `rpc/mixins/` defines no mixin; it is filesystem sweep I/O sitting in the "thin adapter" RPC leaf.
- `architecture.md:129-148` mixin method table is ~half wrong: `get_library`, `sync_library`, `launch_game`, `kill_game`, `get_store_status`, `rotate_device_key`, `get_downloads`, `get_play_sessions`, `get_ui_state`, `set_locale`, `get_cloud_failures`, `retry_cloud_sync` do not exist as RPC methods.

**Re-derived 2026-08-25, bullet by bullet. Four resolved, one inverted, one deferred.** This section's site lists were wrong in both directions more often than any other in Part 2.

1. ~~`parse_launch_options` is dead code.~~ **INVERTED — see §2.9.** True that nothing imports it, and misleading in every way that matters. Split into its own section because it is not a dead-code finding at all.
2. ~~Phantom "handler group" vocabulary.~~ **RESOLVED, and the file list was wrong in both directions.** `auto_wire.py`, named here, is clean. Two sites this bullet never listed were not: `download.py:5` ("two slices that the handler groups split apart", pointing at phantom `StoreHandlers` and `DownloadHandlers` classes) and `action.py:5` ("Mixin equivalent of `ActionHandlers`", contrasting the real composition style against an injected-dependency handler class that does not exist). With `store.py:6`, all three rewritten to describe flat mixin composition onto `Plugin`, each stating outright that there is no `handlers/` package, so the next reader does not go looking. Confirmed by grep that `StoreHandlers`, `DownloadHandlers` and `ActionHandlers` appear nowhere but in those docstrings.
3. **`OP-XX` markers — deferred, and the scale was understated by two orders of magnitude.** Not "threaded through mixin docstrings": **233 occurrences of 149 distinct ids across 162 files**, spanning `event_bus/`, `services/`, `core/` and `stores/`. Comment-only, so a single mechanical commit; kept out of this pass so it cannot drown the review of everything else.
4. **`inject_game_to_appinfo` — deferred by decision, finding corrected.** The bullet omits that it has a **live caller**: `AppDetailsPatch.tsx:140` calls it on every AppDetails open, via `app-store-patcher.ts`'s `injectGameToAppinfo`, which does real work (`forceInjectMetadataForShortcut`) and *then* round-trips to the stub. So this is a wasted RPC per navigation, and it survived §1.2's dead-RPC gate precisely because it has a caller. The stub's own docstring admits it returns `success=True` only so "the frontend's fire-and-forget call doesn't log a failure on every navigation". Also, the persistence it promises looks **redundant rather than missing**, the same shape as §1.1.1's artwork-on-install: `applyAppStorePatch` calls `loadFromBackend` and re-spoofs from the backend cache on every plugin load, so surviving a Steam restart is already handled by re-patching, not by writing `appinfo.vdf`. Whoever picks this up should delete the RPC and the round-trip rather than build the writer.
5. ~~`cleanup_sweeps.py` sits in the RPC leaf.~~ **RESOLVED.** Moved to `core/cleanup_sweeps.py` (`git mv`, no logic change), where `safe_delete`, `marker_sweep` and `stale_installs` already live and two of which it calls. No new dependency class: `core/sync_service.py` and `core/sync_run_mixin.py` already do lazy `services` imports, and `.importlinter` explicitly acknowledges that pattern. Both contracts still KEPT. The validator's carve-out comment went with it: `collect_rpc_methods` filters on `*Mixin` classes, so a helper module in that directory was always skipped by the class filter rather than by an allowlist. Validated through `test_cleanup_full_wipe.py` (89 tests) and `lint-imports`, deliberately **not** by running a real wipe on the dev device: this is the code UD-006 made dangerous.
6. ~~Mixin method table is ~half wrong.~~ **RESOLVED, verified exhaustively.** All 44 method names in the rewritten 17-row table were checked one by one against `async def` in `rpc/`; every one exists. None of the 12 phantom names in the bullet survives.

### 2.9 The launch-options parser is not dead code, it is an unfinished feature

Split out of §2.8, whose bullet reads "`parse_launch_options` — dead, imported nowhere". Nothing imports it, and every other implication of that sentence is wrong. Found by looking for *replicated* logic before deleting, which is the only reason it was caught.

**The module is entirely unimported, not just that one function.** `launcher/types/options.py` is 198 lines with zero importers; `launcher/types/__init__.py` is empty, so nothing re-exports it; and `apply_lsfg_env`, which looked like the live half, has no external caller either. Vulture runs as a **hard** gate over `py_modules/unifideck` and did not catch it, because `min_confidence = 80` reports unused imports and variables but not unused functions or whole unimported modules. That threshold is its own finding.

**Its destination, however, is fully built and consumed in eleven places.** `ParsedOptions`'s fields are mirrored exactly by live dataclass fields that nothing ever writes:

| Field | Consumers | Written by |
|---|---|---|
| `LaunchContext.env_overrides` | `helpers.py:94` and `proton/infrastructure/core.py:384`, both `env.update(...)` applied **last** so a user value beats the plugin's compat env — and covered by `test_proton_prepare_store_env.py:207` | nothing |
| `RuntimeState.wrappers` | 6: `helpers.py:119` (native argv), `generic.py:67`/`:91`, `epic.py:265`, `epic_launch_params.py:207`, `gog.py:295` | nothing |
| `RuntimeState.game_args` | 5: `helpers.py:142`, `generic.py:74`/`:97`, `epic.py:302`, `gog.py:298` | nothing |
| `RuntimeState.lsfg_requested` | none | nothing |

`LaunchContext.raw_options` *is* populated, at all four dispatcher construction sites. `RuntimeState.to_log_dict` already reports `wrappers_count`, `game_args_count` and `lsfg_requested`. Tests already build state with wrappers and args, one of them named `test_wrappers_and_game_args_are_preserved`. And `prepare_windows_plan` returns `tuple[Any, Any]` whose second element is always `None`, with `helpers.py:323` commenting "parsed_options reserved for LSFG/wrapper parsing" and `orchestrator.py:34` documenting an "options" step that never runs. The feature is roughly 90% built and missing one call.

**It is also promised to users.** `docs/launch-options.md:106-127` has a "Planned / not yet wired" section naming this file and specifying the syntax for wrapper words, game args after `%command%`, and LSFG. Deleting the module would silently convert a documented "planned" feature into a removed one.

**There is genuine duplication, and it is not where the audit looked.** Four handlers parse a Steam launch-options string, three of them live:

| Where | Job | Status |
|---|---|---|
| `services/shortcut/launch_options.py` | regex-extract `store:id`; `preserve_user_params` swaps our core, keeping theirs | live (sync/reconcile) |
| `steam-bridge/wrapper-shortcut-launch.ts` `extractUserParams` | strip our tokens, keep user `mangohud`/`gamemoderun`/`#%command%` | live (frontend) |
| `launcher/dispatcher.py:41` `_promote_env_tokens` | promote `UNIFIDECK_*` `KEY=value` from argv into `os.environ` | live (launch hot path) |
| `launcher/types/options.py` | shlex tokenize, all env vars, LSFG overlay, `%command%` split | the missing call |

`_promote_env_tokens` is a 12-line reimplementation of `_tokenize_options` + `_split_env_overrides`, deliberately narrower (only `UNIFIDECK_*`, `setdefault`, never overwrite) but splitting with a naive `raw.split()` where the dead one uses `shlex.split`. No live collision today, because `stores/ubisoft/auth/shortcut.py:33-34` and `buildTemporaryLaunchOptions` both emit space-free unquoted values, but the frontend's `extractUserParams` regex already anticipates quoted values, so it is a latent corruption. The other two are *not* duplicates: they work on different data in different processes and directions, and `is_unifideck_owned` already consumes the first as an injected callback, which is the right shape.

Note also that env-var passthrough works today **without** the plugin: Steam sets `VAR=value %command%` vars itself and the launcher inherits them, which is why the parser was never needed for that half and why its absence went unnoticed.

**Two hazards to handle when wiring it:**

- **`env_overrides` is doing double duty.** `service.py:214` reads `float(ctx.env_overrides.get("started_at", "0"))` from it as a dispatcher-to-service data channel, while two other call sites splat the same dict into the game's environment. Nothing populates it today, so that read is dead and `started_at` is always `0.0` despite a docstring claiming it is "passed in by the dispatcher". A lowercase key cannot arrive from parsing (`_ENV_TOKEN_RE` is uppercase-only), so wiring does not collide, but `started_at` wants its own field rather than growing the overload.
- **Wiring makes `ctx.env_overrides` non-empty for the first time.** That is the feature and its main risk: both consumers apply it *last*, specifically so a user value overrides the plugin's own compat env. `ge-proton11-3-builtin-icuuc-stub` and `ld-library-path-leak-umu-127-libz` are both recorded incidents where that env's precision was load-bearing.

**PARTIALLY RESOLVED 2026-08-25. The env half is wired; the wrapper half was stopped by a measurement taken while wiring it.**

What shipped:

- **`ctx.env_overrides` is populated**, at all four dispatcher construction sites, so a user's `KEY=value` token now reaches the game's environment. Both consumers already applied it last, and `test_proton_prepare_store_env.py:207` already asserted that ordering, so this needed no change to either env builder.
- **LSFG works**, by merging `apply_lsfg_env`'s overlay *under* the user's explicit tokens in the same dict. Putting it there rather than in the env builders is what let both the native and Proton paths pick it up without either growing a parameter. `RuntimeState.lsfg_requested` finally has a writer.
- **The duplicated tokenizer is merged.** `_promote_env_tokens` now splits with the parser's `tokenize_options` (shlex) instead of its own `raw.split()`, so `KEY="a b"` stops promoting a truncated value. Its narrow policy (`UNIFIDECK_*` only, `setdefault`) was deliberate and is unchanged; only the splitting was duplicated. `_tokenize_options` was renamed public for the second consumer.
- **The reserved return slot is gone.** `prepare_windows_plan` returns the plan, properly typed `ProtonLaunchPlan` instead of `tuple[Any, Any]`, and the three call sites plus `orchestrator.py:34`'s "options" claim are updated.
- **The `started_at` overload is gone.** `_build_runtime_state` no longer reads an internal timestamp out of a now-user-controlled dict. It never worked (nothing populated it, nothing read it, and `_launch_started_at` is the real clock), and leaving it would have been a genuine trap.
- **`dispatcher.py` hit the 550-LOC cap** at 584 and was split, not allowlisted: the three functions that interpret the argv tail moved to a new `launcher/argv_options.py`, taking it to 513.

**What was stopped, and why.** `state.wrappers` and `state.game_args` are still unpopulated. `parse_launch_options` was written for a full Steam `LaunchOptions` string, where `%command%` separates wrapper words from game arguments. The dispatcher receives the **post-expansion argv tail** (`" ".join(argv[2:])`), which frequently has no `%command%` left in it, and the parser's fallback for that case moves *every* bare token into `game_args`. Since every argv builder does `argv.extend(state.game_args)`, and the frontend's own `extractUserParams` deliberately preserves the user's `mangohud`/`gamemoderun` into the temp-shortcut options, wiring it produced this:

```
argv tail : UNIFIDECK_UBISOFT_ACTION=auth mangohud gamemoderun
wrappers  : []
game_args : ['mangohud', 'gamemoderun']   ← appended to the GAME's argv
```

That is a launch-breaking regression on exactly the feature the wiring was meant to deliver. Left empty, which is what every release so far shipped. `tests/unit/test_launch_options_wiring.py` pins both halves, so re-wiring it is a deliberate act against a red test, and the worked example above is a test of its own.

#### What Steam actually passes — measured, 2026-08-25

The first write-up of this section said the deciding fact "was not established". It is now, on this Steam Deck, with a logging script as a non-Steam shortcut's `Exe`, driven through `SteamClient.Apps.SetShortcutLaunchOptions` + `RunGame` over CDP. Probe shortcut added and removed per run; `shortcuts.vdf` back to its original 1264 entries.

| `LaunchOptions` | argv the Exe receives | env set by Steam |
|---|---|---|
| `probe-a probe-b` | `[probe-a, probe-b]` | none |
| `PROBE_ENV_A=1 %command%` | `[]` (argc=0) | `PROBE_ENV_A=1` |
| `PROBE_ENV_A=9 %command% tail1` | `[tail1]` | `PROBE_ENV_A=9` |
| `epic:Salt PROBE_ENV_B=2` | `[epic:Salt, PROBE_ENV_B=2]` | **none — not set** |
| `env %command% epic:Salt` | `[epic:Salt]`, under `env` | none (the wrapper ran) |
| `%command% after1 after2` | **never launched** (2 of 2 attempts) | — |
| `ubisoft:upc-auth UNIFIDECK_UBISOFT_ACTION=auth mangohud gamemoderun` | `[ubisoft:upc-auth, UNIFIDECK_UBISOFT_ACTION=auth, mangohud, gamemoderun]` | none |

Four things follow, and they settle the design question rather than restating it:

1. **`%command%` is honoured for non-Steam shortcuts.** Assignments before it are exported by Steam and stripped from argv; tokens after it arrive as argv.
2. **A wrapper word before `%command%` is executed by Steam itself.** `env %command% epic:Salt` ran the probe *under* `env` and still delivered `argv[1] = epic:Salt`. So `mangohud %command% epic:Salt` already works today and needs no plugin code. **`ParsedOptions.wrappers` is therefore not "deferred" but unreachable through this path**: wrapping is a pre-exec act, and by the time our launcher has a `argv` there is nothing left to wrap. `RuntimeState.wrappers` is read by six argv builders that can only ever prepend an empty list. That is phantom vocabulary in the §2.8 sense, and it should be deleted rather than wired — see item 23b.
3. **An assignment *after* the game key is NOT exported by Steam** (`PROBE_ENV_B` arrived as argv and was unset in the environment). Nothing but the launcher can apply it, and before this pass nothing did. So the env half that shipped is a genuinely new capability, not a re-plumbing of something Steam already did.
4. **The `game_args` hazard reproduced exactly**, on the frontend's own temp-shortcut shape (last row). `mangohud` and `gamemoderun` really do arrive in the tail, so populating `game_args` from it really would have passed them to the game. Worth noting they are *useless* there per finding 2, which makes `extractUserParams` preserving them into the tail its own small bug.

One user-facing trap found in passing: **`LaunchOptions` that begin with `%command%` did not launch at all**, twice, while the same string with any token in front of it launched fine. Not chased further, and not caused by this change.

#### Validated by launching a real game, 2026-08-26

Run against `gog:1207658924` (The Witcher: Enhanced Edition, the one installed title on this Deck), driven through `RunGame`, with the evidence read out of `/proc/<pid>/environ` of the **game process itself** rather than from a log line.

| Scenario | `LaunchOptions` | Result |
|---|---|---|
| Baseline (the regression guard) | `gog:1207658924` | Launched to `launcher.exe`, correct per-game prefix, none of the probe variables present |
| User env override | `gog:1207658924 MY_PROBE_VAR=hello WINEDLLOVERRIDES=…` | Both present **on the game process**. `WINEDLLOVERRIDES` arrived with the user's entry first and Proton's appended after it |
| LSFG | `gog:1207658924 LSFG=1` | `ENABLE_LSFG=1` plus all three exports from the real `~/lsfg` (`LSFG_PROCESS=decky-lsfg-vk`, `ENABLE_GAMESCOPE_WSI=0`, `DXVK_HDR=0`) present on the game process |
| LSFG, alternate flag | `gog:1207658924 ENABLE_LSFG=1` | Same overlay, without the bare `LSFG` token. Correct |
| Steam's own env form | `MY_STEAM_VAR=viasteam %command% gog:1207658924` | Game launched, variable **absent from the game** |
| Quoted value (after the `shlex.join` fix, re-run on the deployed build) | `gog:1207658924 MY_QUOTED="alpha beta" LSFG=1` | `MY_QUOTED=alpha beta` intact on the game — it was `alpha` before the fix — with the LSFG overlay alongside |

**That last row is a finding, and it inverts what the docs said.** `docs/launch-options.md` presented `VAR=value %command%` as the mechanism that "works today". It does not, for a Unifideck game that runs under a forced Proton. Steam really does export it — it is present on the outer `steam-runtime-launch-client` process — but the launch then goes through `container_escape`, and `_forwarded_env` (`container_escape.py:143-158`) forwards a variable only when it is in `_ALWAYS_FORWARD` or its value **differs from `os.environ`**. That rule exists to stop container values (notably `PATH`) leaking back, and it is right to have. Its side effect is that a variable Steam exported is byte-identical to the inherited environment and gets dropped with it.

Confirmed in isolation rather than inferred:

```
Steam-set var (equals os.environ)      -> forwarded: False
launch-options var (explicit mutation) -> forwarded: True
ENABLE_LSFG   (explicit mutation)      -> forwarded: True
```

So the route wired in this pass is not a second way to do the same thing, it is the **only** reliable one, and the doc now says so with the measurement attached. The `%command%` form still works for wrapper *programs*, which Steam applies before the escape ever happens.

Not fixed here: making Steam-exported variables survive the escape would need a way to tell a user's variable from container junk, and `_forwarded_env` deliberately has none. Recorded as item 25.

`docs/launch-options.md` now documents the env and LSFG routes under "What works today", and lists wrapper words and game arguments under "Not supported" with the reason, rather than promising them as "planned". The stale "DEAD CODE" note in the `launcher.md` skill is corrected.

**Two lessons.**

*"Imported nowhere" is a fact about the module, not a verdict on the feature.* Before deleting, check whether the destination is built and whether a user-facing doc promises it. This one had eleven live consumers, a reserved return slot, passing tests, and a documented syntax, and the register had it down as one dead function.

*And the converse: code being unreached is sometimes load-bearing.* The parser's `%command%` assumption was silently wrong for its own call site, and nothing revealed that until it ran. Wiring dead code is a behaviour change, so it needs the same measurement a fix does; the fallback branch here would have shipped as a regression on the strength of the code "already being written and tested".

---

## Part 3 — Store logic drift and redundancy

### 3.1 The wrapper/CLI distinction is two hand-maintained tables with no link

- Canonical wrapper identity is a string frozenset in `launcher/wrapper_stores.py:37`: `WRAPPER_STORES = {"ubisoft", "battlenet"}` with predicates `is_wrapper_store`, `prefix_owns_game_install`, `skips_generic_compat`, `uses_manual_download_phase`.
- Each store's `store_info` separately declares `uses_wine: bool` (ubisoft `store.py:65`, battlenet `store.py:70` = `True`; the other four = `False`). There is **no field linking** the two. Today `uses_wine` happens to correlate 1:1 with `WRAPPER_STORES`, but nothing enforces it, and a future wrapper store (EA App) could set one and not the other.

The predicates were deliberately separated ("EA App will be a wrapper store that does not own its installs"), which is sound reasoning, but the identity source-of-truth is still split across `launcher/` and six `store_info` descriptors. `scripts/validate_architecture.py` now machine-checks the `uses_wine ↔ WRAPPER_STORES` agreement.

**RESOLVED 2026-08-26 — the mechanism held, the consequence was wrong, the prescribed remedy was impossible, and the hazard that mattered was the inverse of the one recorded.** Both tables existed as described. Everything that follows from that in this section does not.

- **`uses_wine` had zero readers.** Not in `py_modules/`, `src/`, `bin/`, `tests/` or `docs/` — no attribute access, no dict key, no `getattr`. The frontend's own `StoreInfo` interface (`src/types/api.ts:153`) does not declare it, and the only two fields anything in `src/` reads off that payload are `name` and `display_name`. So the drift this section warns about could not change behaviour in either direction: the two tables were not a source of truth and a copy, they were a source of truth and an echo. §1.1.4's lesson exactly — right mechanism, wrong named consequence.
- **This section's own closing line was stale when it was written.** Check 3 was already a hard gate. It was enforcing agreement on a value nothing read, which is worse than not enforcing it: the presence of a green check is what made the pair look handled.
- **The prescribed remedy (register item 7, "derive the wrapper predicates from a single `store_info` field") is not implementable in that direction.** `wrapper_stores.py` is imported by `launcher/types/context.py` and runs under the system Python (3.10–3.14) in the out-of-process launcher; `test_module_is_stdlib_only` asserts it has zero imports. It cannot read `store_info`. Only the reverse works.
- **`vulture_whitelist.py:126` carried a false justification** — "StoreInfo fields are read through attribute access by every store". Untrue for this member, and the reason a dead-code pass would have skipped it. The same phantom-justification shape as §1.2's DiagnosticsPanel docstrings, in the one file whose job is recording why something *looks* dead.

**The hazard this section missed, and it is the inverse of the one it recorded.** `tests/unit/test_wrapper_stores.py:32-37` parametrized over `WRAPPER_STORES` and asserted **all four** predicates True; line 115 did the same for the prefix-reset guard. That welded `_PREFIX_OWNS_INSTALL` and `_SKIPS_GENERIC_COMPAT` to `WRAPPER_STORES`, so the divergence this section calls "sound reasoning" could not actually happen. The same file asserted both halves of the contradiction: `test_predicates_are_separate_functions_not_one_alias`, 23 lines below, exists to prove they can diverge and names EA App while doing it.

Severity splits by predicate, and only one direction is dangerous:

- `prefix_owns_game_install` forced True is **safe** — it *skips* a prefix reset (`prefix_init.py:199`). Cost is a stale prefix, which Proton's `wineboot -u` upgrades in place anyway.
- `skips_generic_compat` forced True is **not**. It skips winetricks, vcredist and the VC++ registry import outright (`compat/__init__.py:99`). A third wrapper store whose client does not bundle its own redistributables would ship with every game dying on a missing DLL — and the developer would have been pushed there by a red test whose obvious fix is to add the store to the frozenset. Same class as `winetricks-vcrun-installs-dlls-not-msi-state`.

**Where the identity was actually unpinned.** Adding a store to `WRAPPER_STORES` needs a row in five more hand-written places. Exactly one had a pin. A missing row in any of them falls through to the *non-wrapper* default, which is why none of them raises:

| Site | A missing row means | Pinned before |
|---|---|---|
| `wrapper_prefix_probe._SPECS` | the probe never matches | yes |
| `services/prefix_bridge.resolve_prefix:74` | the generic prefix path — cloud saves, size and forensics read an empty directory | no |
| `services/launcher/service._wrapper_handler:306` | `None`: no client opens for sign-in or install | no |
| `launcher/proton/__init__.dispatch:119` | `generic_launch`, which wants an exe the vendor client owns | no |
| `StorefrontLauncher.CLIENT_STOREFRONTS` | the cart button does nothing — no error, no toast | no |

So the one fact that was gated changed nothing, and the five that decide behaviour were not.

**Fix.** `uses_wine` is gone from `StoreInfo` and from all six descriptors; `get_store_infos` injects `client_runs_in_prefix`, derived from `WRAPPER_STORES`, exactly as it already injects `available`. Renamed because the old name did not describe what the gate checked — "Windows-only stores running under Proton/Wine" reads as true for Epic/GOG/Amazon Windows games too, so a maintainer setting Epic's `uses_wine=True` would have been told they were wrong by a check whose reasoning was nowhere on the page.

**Off the dataclass rather than derived onto it, and that is the load-bearing choice.** A `@property` in `core/types/domain.py` would import `launcher/` and break the `types-is-leaf` import-linter contract. Removing the field instead means a re-added literal raises `TypeError` at construction — the duplication cannot come back, and no check is needed to notice. Check 3 keeps its `StoreInfo.name == directory` arm and loses the `uses_wine` arm, which is now unfailable. Per §2.1's lesson, the durable fix deletes every copy but the one a machine checks; here the machine check became unnecessary because the copy became impossible.

The tests are unwelded onto an explicit `_EXPECTED` table with one row per wrapper store and a guard asserting `set(_EXPECTED) == WRAPPER_STORES`, so a new store must state its own answers rather than inherit them. The safe containment (`_PREFIX_OWNS_INSTALL <= WRAPPER_STORES`, `_SKIPS_GENERIC_COMPAT <= WRAPPER_STORES`) stays asserted: a store may be a wrapper without owning its installs, but a non-wrapper store in either set would skip something it genuinely needs.

The four unpinned dispatch sites are now covered. Three are Python and pinned behaviourally in a new `tests/unit/test_wrapper_store_dispatch_coverage.py`, each asserted against the *non-wrapper default* rather than an expected value, so the failure message says "this store silently took the ordinary path". `launcher/proton/__init__.py`'s if-chain became a `_STORE_LAUNCHERS` map so the coverage is inspectable; that is a real behaviour change and it caught one — `test_dispatch_does_not_run_compat` monkeypatched `proton_pkg.epic_launch`, a module name the router no longer reads, so it now patches the map. The frontend's `CLIENT_STOREFRONTS` is unreachable from pytest and became **check 9** in `validate_architecture.py`, replacing the arm that guarded nothing with one that guards a silently-dead cart button.

**Verified against planted violations, not only against a clean tree** (§1.1.4). Adding `"eaapp"` to `WRAPPER_STORES` alone now fails 11 tests plus check 9, naming every site that needs a row; before, it failed the two welded predicate tests and `_SPECS`, and the obvious way to green them was to add the store to both frozensets. Also verified: dropping `battlenet` from either narrow set fails the table row (not a membership assertion), adding `epic` to `_SKIPS_GENERIC_COMPAT` fails the containment assertion, a `StoreInfo.name` off its directory still fails check 3's surviving arm, dropping a row from `CLIENT_STOREFRONTS` fails check 9, and re-adding `uses_wine=` to a literal raises `TypeError` at import.

One thing recorded rather than fixed, because it is a wider finding than this section: `uses_wine` was **one of five** write-only `StoreInfo` fields, not one of one. See register item 26.

**Lesson: a checked duplicate is not a linked one, and a green gate on a dead value is worse than no gate.** This pair had a hard CI check, which is exactly why nobody asked whether either side was read. Before wiring two facts together, check that each has a consumer — otherwise the check becomes the only thing keeping the copy alive.

### 3.2 CLI-store drift (Epic = reference, GOG diverges, Amazon partial)

`shared/cli_install_helpers.py` exists to unify CLI stores but is consumed by only 2 of 3.

- **Binary resolution 2-of-3 break**: Epic and Amazon declare a `CLITool` descriptor and resolve via `StoreBase._find_binary` → `binary_resolver`. GOG hardcodes `bin/gogdl` in `_resolve_gogdl_bin` (`gog/store.py:476-494`), bypassing the resolver, its version check, and its multi-path search.
- **Subprocess/output 2-of-3 break**: Epic and Amazon use `drain_install_output`/`TailRingBuffer`/`wait_with_timeout`. GOG reimplements its own `_read_progress_loop`, two-phase stall watchdog, and `_terminate_gogdl` in `gog/install/progress.py`.
- **Three ETA parsers, three speed parsers, near-verbatim copies**: `shared/cli_install_helpers.py:196/219` (Epic only), `gog/install/progress.py:275/296`, `amazon/amazon_progress.py:23/44`. `amazon_progress.py`'s own docstring admits it was "kept store-local rather than folded into shared" because the shared versions are "close but not byte-identical."
- **Cancellation**: only Epic uses shared `terminate_process_tree`. Amazon re-raises without killing the tree; GOG uses its own `terminate(); sleep(1); kill()`.
- **`StoreBase._run_cli` is dead code** (`store_base.py:161-211`): defined, never called. Every CLI store inlines `asyncio.create_subprocess_exec` instead.
- **Auth/token idioms diverge**: Epic/Amazon share near-identical `_check_*_authenticated` `user.json` readers; GOG and Microsoft instead have full `tokens/` manager subpackages (structurally parallel but not in `shared/`).

**RESOLVED 2026-08-26. One bullet was inverted, one understated a UI hang, and the two costliest defects were not in the section at all.** Every bullet was re-derived against the tree before being worked, per §1.3's lesson, and the cancellation and stall claims were **measured** against the real store code rather than grepped. Verdicts in the original order.

- ~~**Binary resolution 2-of-3 break**~~ **RESOLVED — the mechanism held, the named benefit was phantom, and the real losses were not listed.** GOG did hardcode `bin/gogdl`, `is_file()`-check it and return the path *anyway* on failure. But "bypassing the resolver, **its version check**" names something that does not exist for any store: `BinaryResolver.check_version` has **zero callers** in the tree and `CLITool.min_version` is never compared to anything, so Epic and Amazon get no version check either. What GOG actually lost was (a) the **SHA256 check** — `_log_signature_mismatch` runs only on the resolver's Tier-1 hit, so the `gogdl` entry sitting in `_KNOWN_HASHES` was never compared against anything, and a half-applied update read as "GOG is broken"; (b) the **executable-bit test**, where `_is_executable` skips a file whose exec bit was lost in transit and `is_file()` does not — the exact case `scripts/ensure_executable_bits.py` exists for; and (c) Tier 2/3 fallback.

  **The compounding half was also absent from the bullet.** `GOGStore.is_available` gated on tokens and a userdata probe and never on the binary, so a broken gogdl left GOG showing **connected** — the library syncs fine, being pure HTTP — while every install died at spawn with a raw `OSError` string in the download row. Epic and Amazon both go disconnected, and Amazon additionally converts a spawn failure into a typed outcome. Fixed all three: `GOGStore.CLI_TOOL` + `_find_binary`, an availability gate, and an `OSError` arm around the spawn.

- ~~**Subprocess/output 2-of-3 break**~~ **INVERTED — acting on this as written would have been a regression, and register item 6 said to.** GOG's `_read_progress_loop` is not a reimplementation of `drain_install_output`; it is a **superset**, carrying a two-phase stall watchdog that the shared helper never had. Measured: `drain_install_output` awaited `readline()` with no timeout at all, and `wait_with_timeout` runs only *after* EOF — so a child that is silent for 5s with stdout open returns from the drain at 5s under a 2s envelope timeout. Epic's 7200s and Amazon's 3600s install caps are therefore **unreachable while the CLI holds stdout open**, and since `max_concurrent` defaults to 1, one wedged install blocked every queued game until `plugin_loader` was restarted. That is the class behind `install-hang-orphaned-wineserver-lock` and `install-stuck-broken-proton-experimental-hang`.

  So the drift direction is **up, not down**. Lifted GOG's shape into `drain_install_output` (`stall_s` / `finalize_s` / `in_finalize`); GOG keeps its behaviour through the shared helper, Epic and Amazon gain stall detection they never had. The helper raises `InstallStalledError` rather than killing, so each store keeps exactly one `terminate_process_tree` site instead of two that can drift. **Do not re-file this as "make GOG consume the shared helpers".**

- ~~**Three ETA parsers, three speed parsers**~~ **RESOLVED — and the docstring reason not to unify describes an unreachable divergence.** `amazon_progress.py` declined to fold in because the shared versions were "close but not byte-identical … (nile's sign handling differs)". Measured over 23 vectors of real legendary/gogdl/nile output: **the three ETA parsers are identical on every vector, and the three speed parsers agree on every real CLI line.** They diverge only on `+ Download -9.75 MiB/s` (unspaced sign) and `Download 5.00 MiB/s` (no `+`), neither of which any of the three CLIs emits.

  One real defect fell out of the diff: shared and GOG returned a **negative** transfer rate for the unspaced form, which would render as a negative MB/s. Amazon's `lstrip("-")` was the only correct one of the three and is the one kept. Also merged the transfer-line parser itself: gogdl and nile emit the *same* `Progress: <pct> <written>/<total> … ETA:` line, so the two store copies were one function all along. `amazon_progress.py` deleted (89 lines); `parse_progress_line` renamed `parse_percent_re`, because Amazon was importing two different functions of that name and aliasing one at the import, which is a large part of why the duplication went unnoticed. `tests/unit/test_cli_progress_parsers_agree.py` pins the vector table so the equivalence claim stays checked rather than remembered.

- ~~**Cancellation**~~ **RESOLVED — this was the highest-value item in §3.2 and the register understated it by a wide margin.** "Amazon re-raises without killing the tree; GOG uses its own `terminate(); sleep(1); kill()`" is mechanically right and reads like tidy-up. Measured, driving the real store code against a live child that forks a worker, with Epic as the control:

  ```
  EPIC   : tree [37444, 37445] -> after cancel, ALIVE: []            clean, 0.00s
  GOG    : tree [37357, 37358] -> after cancel, ALIVE: [37357,37358] *** LEAKED ***
  AMAZON : tree [37390, 37391] -> after cancel, ALIVE: [37390,37391] *** LEAKED ***
           cancel STILL BLOCKED after 6s   (bound is _install_timeout = 3600s)
  ```

  **What the user experienced.** Press Cancel; the row flips to Cancelled; the download tool keeps running — parent *and* its `multiprocessing` workers (gogdl spawns `Process` in `dl/workers/task_executor.py`; nile is a PyInstaller ELF carrying `multiprocessing`). So bytes keep landing after "cancelled" and free space keeps falling with no visible download; a cancel-then-uninstall deletes a directory another process is still writing into, and files reappear after the uninstall reports success; an orphan can still write the completion manifest, the exact shape `launcher/wrapper_prefix_probe.py:22` records for Battle.net; gogdl leaks a `/dev/shm` segment per orphan; and a retry runs a second downloader over the same directory. GOG's own `_terminate_gogdl` could not have helped even when reached: it killed the parent only, and its `await asyncio.sleep(1)` is skipped outright in a task already being cancelled — the precise failure `terminate_process_tree`'s docstring documents and guards with `contextlib.suppress`.

  **Amazon's second defect was not in the register at all: cancelling hung the UI.** It caught `CancelledError` into `drain_exc`, then ran `wait_with_timeout(proc, 3600)` *before* re-raising. `DownloadService.cancel` awaits the task and the RPC awaits `cancel`, so the whole call blocked until nile finished or an hour elapsed. Epic has had the same two steps in the opposite order the whole time. Fixed by transposing them — the leak and the hang are **one** bug, not two.

- ~~**`StoreBase._run_cli` is dead code**~~ **RESOLVED — deleted (register item 10).** Zero callers confirmed; `stores/shared/cli_credentials.py:21` already documented it as dead. Deleted rather than adopted: every CLI store needs to *stream* stdout for progress, which a collect-at-the-end helper cannot do, so adopting it would have meant rewriting it anyway. A comment in its place says so, and points at `core.binaries.clean_cli_env` for the one thing it did provide. **Note a claim this bullet invites that is wrong:** bypassing `_run_cli` did **not** leave GOG with an unscrubbed environment — every GOG subprocess site goes through `acquire_gogdl_creds` → `clean_cli_env` (`gog/tokens/gogdl_credentials.py:84`). No change was needed there.

- ~~**Auth/token idioms diverge**~~ **RESOLVED for the `user.json` half; the `tokens/` half was already closed.** The two readers were ~35 structurally identical lines differing in exactly three places — the store label, the config key, and the final predicate — now `stores/shared/cli_credentials.read_cli_user_json`. The risk is a change to one copy and not the other, which has already happened once: `nile` is pinned at 1.1.2 precisely because 1.2.0 deletes the `user.json` the Amazon copy reads, and nothing structural connected that constraint to Epic's identical reader. Sharing also gained Amazon an object check it lacked — it called `data.get` on whatever `json.load` returned, so a `user.json` holding a JSON *array* raised `AttributeError` out of the store-status path. The GOG/Microsoft `tokens/` half of this bullet is largely stale: register item 9 already extracted `security/token_file.EncryptedTokenFile` and both consume it. What remains is package *shape*, not duplicated logic.

**Found while resolving this section, not in the register:**

- **A failed GOG install never said why** *(highest user-facing impact in §3.2)*. Epic and Amazon fold the CLI's output tail into the error (`legendary_exit_1: …`, `nile_exit_1: …`) — both were deliberate fixes for reported silent failures (`ud026-epic-install-error-surfacing-and-dlc-fallback`, `amazon-nile-exit-1-error-surfacing`). GOG never got one: `run_gogdl_with_progress` returned a plain `bool`, so the installer could only report one of five bare codes (`gogdl_not_found`, `not_authenticated`, `download_failed`, `install_not_located`, `marker_write_failed`). None is an i18n key, and `friendlyDownloadError` falls through to echoing the raw string — so the toast and the failed row showed the literal token **`download_failed`** in all 16 locales. Worse, that classifier branches on the *message text*, so a GOG install dying from a full disk, a dropped connection or an expired token could never match the disk / network / auth branches Epic and Amazon hit for the identical cause. Fixed with a `gogdl_exit_{rc}: {tail}` outcome — `EXIT_PREFIX_RE` in `src/lib/download-errors.ts` has stripped exactly that prefix all along and its docstring cites `gogdl_exit_2:` as an example, a format nothing in the tree ever emitted (phantom vocabulary in the §2.8 sense, on the frontend). Zero new strings, zero locale churn.

- **Four `errors.download.*` keys were translated in all 16 locales and referenced by nothing in `src/`** — `toolMissing`, `directoryNotFound`, `processFailed`, `lockConflict`. The same shape as §1.1.2 and the `SYNC_SKIPPED` finding: the strings were written and translated, the delivery was never built. `lockConflict` in particular is an exact fit for `legendary_install_lock_busy`, the phantom-install failure of `legendary-exit0-lock-refusal-and-cancel-orphans`. All four are now reached by a code table in `friendlyDownloadError`.

- **`BinaryResolver.check_version` has zero callers project-wide**, and `min_version` is never compared. Left in place rather than deleted — unlike §1.2's RPC surface this is a library API with a plausible caller, and the honest fix is either to call it at resolve time or to drop `min_version` from `CLITool`. Recorded so the next reader does not cite it as a working safeguard, which is what this audit did.

**Guards.** `test_cli_install_cancel_and_stall.py` (cancel + stall for all three stores, including the negative case that a live heartbeat is never killed, and an explicit assertion that Amazon's cancel *returns promptly* — without which it passes against the blocking original), `test_gog_install_error_surfacing.py`, `test_cli_progress_parsers_agree.py`, and `src/lib/download-errors.test.ts`. All were **verified against planted violations** (§1.1.4's lesson), ten in total across both batches, every one caught. One planted-violation run found a real bug in the new frontend code: a `_not_found$` pattern was swallowing `install_path_not_found`, which is a missing game directory, not a missing CLI.

**Lesson for the rest of this register: a drift finding names a difference, not a direction.** Bullet 2 was a true observation of divergence whose implied remedy — make the outlier match the others — would have deleted the only stall detection in the tree and re-opened two recorded incidents. Before consolidating on the majority implementation, check which one is actually the better one; here the single store was ahead of the shared helper, and the shared helper was the thing that needed to move.

### 3.3 Wrapper-store drift (Ubisoft mature, Battle.net consumes shared, half-migrated)

~~Ubisoft keeps a private duplicate of what Battle.net gets from `shared/`; migrate it.~~ **RESOLVED — the observations mostly held, the remediation did not, and the two highest-value defects were not in the section.** Re-derived from the tree before being worked, per §1.3's lesson. Of the three migrations register item 8 named, **one was partly right, one was blocked on data nobody had measured, and one is now declined on the record.**

**The one list that was entirely correct** is what is genuinely shared: `wrapper_install`, `prefix_placement`, `wrapper_auth_monitor`, `installed_size.dir_size_bytes` and `wine_path` each have a live consumer in both stores. Verified by import graph.

**Prefix cloning — one of three claims was wrong, and acting on it would have deleted a repair path.**

- `rsync_clone` **was** a real duplicate, and the copy was missing the flag that matters. `shared/prefix_clone.rsync_clone` takes `checksum=`, and says why: *"Required when repairing an existing prefix: identity files are small and are frequently rewritten within the same second at the same length, so the quick check skips them and the repair silently does nothing."* Ubisoft's copy had no such parameter, and **three of its five call sites write into a tree that may already be populated** — including `prefix/manager.py`'s game-prefix identity repair, the one clone that can *never* delete its destination first because the game's files live inside the prefix. Fixed by deleting the copy and routing all five sites onto the shared function, with `checksum=True` on the three repairs and the default left on the two fresh clones (an empty destination makes the quick check both correct and faster).
- `fix_pfx_symlink` **was not a duplicate of anything.** Its guard is `if not is_symlink(): return` — it *retargets a wrong existing* link. `prefix_clone.ensure_pfx_symlink`'s guard is `if is_symlink() or exists(): return` — it *creates a missing* one. Opposite conditions; neither can do the other's job, and migrating as the register asked would have removed the only repair for a mis-targeted link. Both docstrings now say so, so neither reads as dead beside the other. The §1.4 `resolve_proton_path` shape inverted: there two same-named functions really were incompatible, here two differently-named ones were filed as duplicates because they touch the same file.
- `write_bootstrap_marker` **was a real duplicate with incompatible on-disk formats** (Ubisoft plaintext lines, Battle.net JSON). Unified onto `PrefixMarker`, which gained an optional `game_id` so Ubisoft's `game=<space_id>` survives. **Two constraints the register did not mention, both able to lose user-visible state:** the *filename* must not change, because `compatdata_scan.MARKER_PREFIXES` proves prefix ownership and matches `unifideck_ubisoft_bootstrap.marker` through its `unifideck_` arm alone; and no Ubisoft path may move to `is_owned_by`, because `read_marker` maps an unparseable file to `store=""` and the strict test therefore reports **False for every prefix already on a user's disk**. All five Ubisoft readers test `is_file()` and none parses the content, which is exactly what makes the content switch transparent — and what makes the `is_owned_by` upgrade a separate job (item 26).

**Session capture/purge — true as a fact, wrong as a remediation.** `wrapper_session_specs.SPECS` has exactly one row, `battlenet`. `spec_for("ubisoft")` returns `None`, so `WrapperSessionHooks` is **inert for Ubisoft**: every method returns `False`/`0`. Migrating would have replaced working behaviour with a no-op. It is also not an equal-but-different implementation — Ubisoft's is a **superset**, covering the same three moments (`store.py`'s stop hook, `installer/uninstall.py:129`'s capture before deletion, `auth/facade.py:226`'s purge on sign-out) and adding `propagate_all_to_all` after a capture plus `stored_credential_was_rejected` reporting a server-dead token to the user. Doing the migration properly means first *measuring* a UPC `SessionSpec`; `wrapper_session_specs` records what guessing costs (`ERROR_TOKEN_NOT_FOUND (49)` — the files arrived and the token did not). **Declined, with the reasoning written into `wrapper_session_hooks.py` so it is not rediscovered as drift.**

**The one real gap here was not in the section.** Ubisoft had no equivalent of `_await_client_exit`. The shared hook waits, bounded, for the vendor client to exit before reading, because *"the client flushes its rotated token on shutdown … reading too early gets a torn vault."* Ubisoft read the instant `GAME_STOPPED` fired, and its `_is_valid_css` guard is size-only, so a long-enough partial write passes. Worse downstream than for Battle.net: Ubisoft follows a capture with `propagate_all_to_all`, so one torn read reaches *every* Ubisoft prefix. Fixed by extracting the wait as a free function `await_client_exit` — reuse rather than inheritance, since subclassing the mixin would have auto-wired a second, no-op `GAME_STOPPED` subscriber beside the real one.

**Auth shortcut — true, and now declined on the record.** `shared/auth_shortcut.py` was already arguing for this in its own docstring ("a follow-up that wants device testing, since it is a shipped and working auth path"). Ubisoft's 582 lines carry behaviour the shared module has none of: legacy `ubisoft:.template` pruning, orphan `upc.exe` pruning that declares its deletions to the shortcuts write guard by appid, `validate_auth_shortcut` field self-heal, compat-tool clearing, registry ops. Migrating moves ~580 lines into `shared/` for one consumer, on the store with the longest incident history in sign-in. The docstring now states the decision rather than deferring it.

**`prefix_forensics` — filed as redundancy, and it was a live lost-diagnostics bug** *(highest impact in §3.3, and absent from the register line)*. `VENDOR_LOG_GLOBS` **already carried a complete, measured `"ubisoft"` row**, and `support_bundle/sources.py` already collected `launches/*.vendor.txt` store-agnostically — while **nothing in `stores/ubisoft/` ever called `preserve_vendor_logs`**. Meanwhile `installer.py`'s `_cleanup_abandoned_prefix` deletes the prefix on every failed *and* cancelled install, and for a wrapper store the prefix **is** the install. So UPC's own account of why it would not install went with it, every time, and triage had to reconstruct from timing across other logs — the cost `prefix_forensics`' docstring already records as having consumed one field investigation for Battle.net. Exactly the §1.1.2 / §1.3 shape: *the material shipped and was documented; only the delivery channel was missing.* Fixed with one call, zero new data.

**`wine_env` — correctly Battle.net-only.** It is a four-line delegation to `UbisoftBinaryResolver`, so Ubisoft consuming it would be circular. §1.4 already corrected the related "duplicated `find_umu_run`" claim. No change.

**`install_base.py`'s "second consumer" claim was false** and is corrected: only `ubisoft/config.py` imports `detect_sdcard_install_base`. The generalisation is still worth keeping; it is just not evidence of two callers.

**Found while re-deriving, and deliberately left alone: a *third* clone.** `launcher/proton/handlers/ubisoft_recovery.py:138 clone_template_into` is last-resort recovery, and folding it into `shared/prefix_clone` would have broken it three ways: it runs in the launcher under the system Python and is synchronous; it needs a `cp -a` fallback the shared version lacks; and it **restores the target's own `.unifideck_proton_version` marker across the copy**, an on-device fix from 2026-08-01 that stopped a permanent reset→re-clone→revert loop (Rayman Origins), pinned by `test_ubisoft_template_clone_marker.py`. The §1.4 "write-only install manifest" class again: acting on the finding causes the loss. Its docstring now says why it is not the shared one.

**Guard: check 10** in `validate_architecture.py` — every store declaring `VENDOR_LOG_GLOBS` must call `preserve_vendor_logs` **from its own package**, so one store cannot vouch for another (a whole-tree grep found Battle.net's call and read as covered, which is how Ubisoft's absence survived a release). Opt-out `# no-vendor-salvage: <reason>`, exemption count printed on every clean run. Verified against planted violations in both arms, not only against a clean tree. All eleven planted reversions of this pass's own fixes were confirmed caught by its tests.

**Lesson for the rest of this register: "X is duplicated in A and B" is three separate claims — that A and B do the same thing, that sharing is possible, and that sharing is an improvement.** Here the first failed once (`fix_pfx_symlink`), the second failed once (`wrapper_session_hooks`, whose shared path is inert for the store being migrated onto it), and the third failed once (`auth_shortcut`, a single-consumer move onto the highest-incident path). Check each separately before writing a migration line — and check what the shared module *already declares* for the store, because twice here it declared more than the store used.

### 3.4 Cross-store copy-paste redundancies

**RESOLVED — three of five bullets closed here; two were already closed and the section had not been re-read.** Validating each bullet before acting on it was the whole value of the pass: two needed no work, one had a live defect the bullet did not mention, and one was miscounted in a way that would have destroyed working behaviour if the count had been trusted.

Per-bullet outcome:

- **`merge_install_status` — 3 real copies, and one of the three divergences was a live defect.** Consolidated onto `stores/shared/install_status.py`. The three copies were run against identical inputs before anything was changed, which is how the defect surfaced: Epic's and Amazon's disk-existence check was itself guarded on the path being truthy (`if install_path and not Path(install_path).is_dir()`), so **a record with a missing or empty path skipped the check and was marked installed anyway** — the exact failure the check exists to prevent (Steam showing PLAY for a game with no files, the reported Vampire Survivors case). Reachable on Amazon: `amazon_library.read_installed_ids` defaults the key to `""`, so a nile record without `path` produced `installed=True, install_path=""`. Converged on strict for all three stores.

  The other two divergences are **deliberate and survive as arguments**, and one of them is why "make the outliers match" would have been wrong here too. GOG carries its scanned `executable` onto `exe_path`; Epic and Amazon must not, because legendary's equivalent field is a **relative Windows path** (`core.py:711` does `.replace('\\','/').lstrip('/')` before joining it onto `install_path`) — generalising GOG's behaviour would have written a relative path into `games.map` and broken launch on the two most-used stores. GOG also skips the disk re-check, correctly: its map comes from a live `iterdir`, so there is no separate record that can outlive the files. Both are now `exe_key=` / `verify_dir=` with the reason recorded at the parameter.

  Two smaller findings in passing. Epic's "accept a nested `install` dict too for older call sites" fallback was **dead**: legendary emits `vars(InstalledGame)` (`cli.py:302`), which is flat, and no call site has ever produced the nested shape. And all three copies hand-listed all twelve `Game` fields, so a field added to the dataclass would have been silently dropped by three call sites at once; the shared helper uses `dataclasses.replace`.

- **`_rebuild_auth_after_injection` — 4 copies, not 5, and the fifth must never be migrated.** AST-normalised, Epic's, GOG's, Amazon's and Microsoft's were statement-for-statement identical apart from a store-name literal, a log prefix, the auth-flow constructor, and one extra call in GOG's. Those four now share `stores/shared/browser_auth_rebuild.BrowserAuthRebuildMixin`. **Ubisoft's same-named method is not a copy of that body at all** — it is a different implementation of the same `store_injector` protocol (which finds the hook by `getattr` on the instance), wiring the just-injected shortcut service into an auth facade for a store that has no browser monitor. Consolidating it, as "5 copies" invites, would have deleted the fix for "Auth shortcut not available" blocking sign-in entirely.

  The mixin is applied to the four stores and deliberately **not** to `StoreBase`: the injector's only test is whether the attribute exists, so a base-class implementation would start firing for Battle.net and Ubisoft. GOG's extra call is now `_after_auth_flow_built`, the single behavioural difference between the four, and the highest-risk line in the change — without it `_gogdl_bin` stays empty, `is_available` refuses, and every GOG install dies at spawn. Also removed: `amazon_store.py`'s `self._bus_ref`, an alias of the `self._bus` that `StoreBase.__init__` had already set, read only by the two lines inside the copy; and three hardcoded store-name literals, now `self.store_info.name` (already machine-checked against the directory name by check 3).

  No live user-facing bug in this bullet — `start_auth` already returns a structured `auth_not_configured` when `_auth` is None.

- **`_check_*_authenticated` — already closed; the bullet was stale.** Both stores call `shared/cli_credentials.read_cli_user_json`, landed with register item 12. No work.

- **The prefix-clone/pfx-symlink/marker trio — already closed by the §3.3 pass**, including the finding that `fix_pfx_symlink` is *not* a duplicate (its guard is the inverse of `ensure_pfx_symlink`'s). No work.

- **`dir_size_bytes` — a real duplicate, and the performance claim it invites is wrong.** `compatdata_scan._dir_size_bytes` (a `Path.rglob` walk) deleted in favour of `shared/installed_size.dir_size_bytes` (an iterative `scandir` walk, already imported by `services/installed_disk_info`). Measured over 28 real prefixes on the dev Deck — 21 Steam `compatdata` dirs, both wrapper-store templates and auth prefixes, and a 2.4 GB per-game prefix — the two are **byte-identical on every one**, and the scandir walk is 3–4× faster per directory.

  Two things worth recording so they are not re-litigated. First, **neither implementation follows directory symlinks**, so the plausible-sounding hazard (a compatdata prefix contains `dosdevices/z: -> /`, so an `rglob` walk would size the whole filesystem) does not exist: `pathlib`'s `**` has skipped symlinked directories since long before 3.13's `recurse_symlinks`, and the identical totals prove it empirically. Second, **the boot-time saving is ~0.1 s, not seconds.** `reclaim_redundant_compatdata` runs `scan(with_sizes=True)` at boot, but `_is_scannable` restricts it to non-Steam appids — 3 directories on this device, not 22 — measured 0.14 s → 0.04 s, inside an `asyncio.to_thread`. The value of this bullet is one fewer implementation of "how big is this directory", reachable from two different UI surfaces that must agree; it is not a performance fix, and describing it as one would be dishonest.

Guarded by **check 11** in `validate_architecture.py`: each promoted helper is pinned to its owning module, with `# intentional-divergence: <reason>` as the opt-out (one live consumer — Ubisoft's hook). Verified against three planted violations: Epic's copy re-added, a `SHARED_HELPERS` row whose owner moved away, and Ubisoft's marker removed. Net effect on production code: **−164 lines**, with two new shared modules of 107 and 100 lines replacing five deleted copies.

Drive-by in the same area: `core/compat_bridge.py:66` claimed `to_unsigned` "mirrors `services/shortcut/orphan_scan._to_unsigned`". That function no longer exists — `orphan_scan` imports this one. Docstring corrected.

*(awaiting user device validation — TV-1…TV-12 in the plan)*

### 3.5 Partial-implementation flags

- **microsoft** (xCloud) is correctly stub-shaped for a subscription store, but its `install_game`/`uninstall_game`/`update_game` return `success=True` no-ops (`microsoft_store.py:383-424`) rather than an explicit `not_supported`.
- **battlenet** lacks (relative to Ubisoft): pre-install `get_game_size` (relies on `product.db` populated only at completion), `check_for_updates` (intentional, client self-updates), web-library fallback, achievements, playtime, DLC.
- **ubisoft** lacks (relative to Epic/GOG): `check_for_updates` (`[]`), `get_game_size` (`None`), achievements, playtime.
- **amazon** lacks DLC, achievements, playtime.

**RESOLVED 2026-08-26 — and this section is the least useful one in the register, for a structural reason worth naming.** It is a *capability inventory*: four bullets of "store X lacks Y", with no impact attached to any line. Re-derived against the tree, **three of the four bullets have no user-facing symptom at all** — every gap they name is gated before it can fail. The one bullet that matters names a different defect than the one it records. And the two most serious problems in this area are not in the section, because a list of what is missing cannot see a feature that is *present and unreachable*.

**Bullet 1 — microsoft's `success=True` stubs. Mechanism held; unreachable; a trap rather than a live bug.** The three methods were as described. What kept them harmless was not `store_info.supports_install=False` — that field has **zero readers** anywhere in `py_modules/`, `src/` or `tests/` beyond one assertion, so it gates nothing (register item 26's finding, fifth field). Install and update had two real guards: `usePlaySection.ts:66` short-circuits any `xcloud`-tagged game before the `not-installed` branch, so the only Install button in the UI never mounts; and `DownloadWorker._execute_install` carried its own store-name rejection ahead of the dispatch. **`uninstall_game` had no backend guard at all** — only the frontend's `is_installed` gate — so it is the one of the three a caller could actually reach, and the one whose `success=True` would have let a shortcut flip out of an install state it never held.

Fixed by refusing: all three return `success=False` with `error`/`error_code` `not_supported`. `^not_supported$` maps to the existing `errors.download.generic` in `download-errors.ts` — zero new strings, zero locale churn — so a refusal can never surface as a bare token, which is §3.2's `download_failed` defect. **The worker's rejection was deleted rather than kept**, and that is an improvement in its own right: it bypassed `_emit_failure`, so the queue row never reached "failed" and never got an `error`, while the toast echoed a hardcoded English sentence straight past `friendlyDownloadError`'s code table. One refusal, in the store, now yields correct row state, one classified error and one translated toast. Two more phantoms went with it: the `item.store == "microsoft"` arm in `_run_prefix_warmup` (success-path only, so unreachable once install is refused) and `GameInfoCompatRow.tsx`'s exclusion of Microsoft games tagged `not_compatible` — **a tag no backend has ever emitted**, existing only in that one expression, its `as never` cast the tell (§2.8's class, on the frontend).

The guard that matters is `usePlaySection.test.ts`, because that `if` had none. Verified against a planted violation: moving the `xcloud` branch below the `not-installed` branch — the natural way to break it — fails 3 tests. The next Microsoft feature, PC Game Pass, is exactly a Microsoft `Game` *without* the tag, which is why the backend must refuse rather than trust the hook.

**Bullet 2 — battlenet. Two claims correct and unfixable, one intentional, one wildly understated.**

| Claim | Verdict |
|---|---|
| pre-install `get_game_size` | **True, and not fixable locally.** `product.db` populates the total only at completion, and `CatalogEntry` (`ownership/pub_catalog.py:66`) has no size field. `None` pre-install is the honest answer, not a gap. |
| `check_for_updates` (intentional) | **True and correct**, with a docstring explaining why a guess produces phantom update badges. |
| web-library fallback | **Understated to the point of inverting the finding — see finding A.** |
| achievements / playtime / DLC | **True, zero symptom** — see bullet 4. |

**Bullet 3 — ubisoft. Both named gaps are real; one has a consequence the bullet does not draw.** `get_game_size → None` is correct and unfixable locally (the parsed `GameConfig` carries no size). `check_for_updates → []` is true — and because `get_available_updates` is fed from `update_check_cache` and therefore from it, **Ubisoft's entire working update path is unreachable from the UI**: `update_game` → `UbisoftInstaller.update_game` → `update_op.update()` can never be triggered. Its own docstring already concedes the window would not render in Gaming Mode if it were. Marked `# unwired:` and split out as a register item rather than deleted — the code works, and UPC updates are a real user need. Same "implemented, no trigger" shape as §1.1.1.

**Bullet 4 — amazon. All three true as capability statements, all three with no symptom, and one is the reverse of what it says.**

- **Achievements** — gated twice: `_ACHIEVEMENT_STORES = ("gog","epic")` in `rpc/mixins/achievements.py:27`, and a hardcoded duplicate of the same list at `GameInfoCompatRow.tsx:195`. Two hand-maintained tables in two languages with no link — §3.1's exact shape, unpinned by any check. Recorded as a register item.
- **Playtime** — the bullet's phrasing is wrong. Local playtime tracking is store-generic (`services/playtime/service.py` keys off `GAME_LAUNCHED`/`GAME_STOPPED`) and works for Amazon. What these stores lack is playtime *push-back to the storefront*, gated by `playtime_sync/service.py:47`. No CLI or client here exposes an endpoint for it.
- **DLC** — inverted. **No store has a DLC surface.** `GOGStore.get_game_dlcs`, `install_dlc` and `get_game_store_url` have **zero callers project-wide** — no RPC route, no frontend — and `shared/dlc._DLC_SUPPORTED_STORES` lists Epic and GOG while only Epic consumes it, as `store_supports_dlc("epic")`: a literal checked against a set containing it, i.e. a constant `True`. "amazon lacks DLC" reads as a per-store gap and is a feature nobody has. Marked `# unwired:`, registered as one unit.

**Finding A, not in the section: every Battle.net free-to-play and subscription title is permanently invisible.** `BattlenetStore._cached_game_accounts` reads `cache.get("battlenet", "game_accounts")`. **Nothing in the tree ever writes that key** — the only other reference is a `clear()` on logout, and `git log -S` shows one commit, the initial Battle.net integration: the consumer shipped, the producer never did. So `game_account_programs` is always empty, `rules._match_game_account` can never match, and every title whose catalog rule keys on `game_account` rather than `license_id` is dropped — precisely the free-to-play and subscription set. `library.py`'s own header measures it on a real account: *17 programs from licences, 22 with game accounts*.

Two things hid it, both already-named shapes from this register. The consumer's docstring described the producer as if it existed — *"Supplied by the optional web enrichment; empty is safe"* — the phantom-justification shape of §3.1's vulture comment. And `test_battlenet_ownership.py:136` proves the rule engine handles game accounts *by hand-building the facts*, so it passes while production is unconditionally empty: §1.1.1's "both guards locked in opposite halves" again. **Correct machinery fed an always-empty input is a missing feature that no assertion on the machinery can see.**

Not fixed here, deliberately: the producer needs the session token, which for this store is a **Wine registry key**, and building it is a feature with its own device validation. What landed instead makes the gap *measurable* — `library.count_game_account_gated` re-evaluates the catalog assuming every program has a game account and logs the difference each sync, the two false docstrings now say what is true, and `test_battlenet_game_account_gap.py` asserts the cache key still has no writer, with a message telling the next reader to delete the test when it does.

**Finding B, not in the section and the most serious item in Part 3: a store that could not answer had *all* its shortcuts deleted.** The protection existed and was documented — `stale_predicate.is_stale_managed_shortcut` describes `valid_stores` as *"how staging avoided nuking the user's Epic shortcuts after they logged out of Epic"* — and `shortcut/events.py` overrode it, passing **every registered store**. It received `errors` in the same payload and ignored it.

A store contributes zero games without owning zero games in four ways, and each deleted every shortcut it owned:

- it **raised** — `_sync_one_store` catches everything and returns `[], str(e)`;
- it **timed out** — `_fetch_one` returns `[], "timeout"`, the likeliest case on a slow link;
- it was **unavailable and never fetched** — `_run_sync` iterates available stores only, so it records **no error at all** and was invisible in the logs;
- it returned **`[]` silently** — `None` was mapped to `[]`, so "I could not read" and "you own nothing" were the same value.

Battle.net was most exposed: both its unreadable cases returned `[]` without raising, so it never even reached `errors`. **And the §3.2 pass widened the blast radius without anyone noticing:** GOG's `is_available` now refuses when `bin/gogdl` is missing or non-executable — correct in itself, and the reason it was added — so a half-applied update that lost the exec bit made the next sync delete **every GOG shortcut in the library**. A live regression path created by a fix, which is §1.1.4's warning from the other direction.

Fixed by replacing the rule with the invariant it should always have been: **a store's shortcuts may be swept only when we hold a current authoritative statement of what it contains** — fetched this run, and answered without error (`events._sweepable_stores`). Battle.net's two unreadable paths now return `None`, reported as `library_unreadable`. `registered_stores` was **removed** from the `SYNC_COMPLETE` payload and its schema row rather than left unread, per §3.1's lesson: a payload field whose only consumer stopped reading it is an invitation to re-adopt it.

Deliberately preserved, and it is the half a careless fix would lose: a store that answers with an **empty** library is still swept, which is the phantom-Ubisoft cleanup the original widening existed for. The one artifact that cannot be reached that way — the 0.6.x `microsoft:ms-auth` row, whose owner is a store the affected user has usually never signed into — is swept **by name** via `protected.LEGACY_SWEEP_IDS`. Naming one dead row is the narrow fix; gating its whole store on availability was the wide one.

Both halves are pinned in `test_sync_failed_store_keeps_shortcuts.py`, asserted on the surviving *entry* rather than on "reconcile ran" — the broken version ran reconcile too, it just deleted the row. Verified against two planted violations (restoring the old rule fails 5 tests; dropping the legacy hatch fails 1). Two files were at exactly the 550-LOC cap, so per rule 5 the predicate moved to `services/shortcut/stale_predicate.py` and Battle.net's library read to `library.read_library` — both pure moves, no logic change.

**Lesson: an inventory of what is missing cannot see what is present and unreachable, and those are the expensive ones.** Every gap this section listed was already gated — the cost of a gap you have thought about is bounded. What it could not list was a live consumer with no producer (finding A) and a working guard overridden at its only call site (finding B), both of which read as *implemented* from any angle except tracing the value end to end. When a section is a list of absences, the question to ask is not "which of these should we build" but "what is here that does not work".

---

## Part 4 — Remediation register *(FROZEN 2026-08-26 — see `docs/audit-register.md`)*

> **Do not tick boxes here.** This section is frozen as the historical record of
> how each item was resolved; the reasoning in the resolved rows is worth
> keeping and is not repeated in the tracker. Live state lives in
> `docs/audit-register.md`.
>
> Three things below are known-broken as identifiers and were fixed in the
> tracker, not here: item `26` appears **three** times and `27` **twice**
> (different findings each), and `24`/`24a` are unrelated topics. The tracker's
> "ID rules" table maps the old numbers to permanent ones — cite those.

Ordered by risk-to-value. Tick the box when a fix lands and the user has validated it.

### P0 — silent correctness bugs (fix first; make dead paths actually run)

- [x] 1. ~~Fix the `GAME_INSTALLED` payload~~ → **the event had no live emitter at all; retired it entirely.** Repointed `collection-manager.ts` at `SHORTCUT_INSTALL_STATE_CHANGED` (fixing a real user-facing bug this audit missed: the `[Unifideck] Installed` collection never picked up installs), deleted both dead subscribers, the dead discovery half of `core/manifest.py`, the whole ProtonService compat-tool path, and 2 vulture allowlist entries. See the rewritten §1.1.1. *(awaiting user device validation)*
- [x] 2. ~~Wire `MetricsCollector` timers/gauges~~ → **already fixed by commit `8b62be5`, before this register was worked.** Verified against both the current tree and `8b62be5^`; the line had simply never been ticked. Two *other* metrics defects were found and fixed while resolving item #4 — see §1.1.3 and item #4.
- [x] 3. ~~Resolve `TOAST_NOTIFICATION`~~ → **retired it; all three emitters now use `LAUNCHER_STAGE`.** The channel was dead for three independent reasons, not one — the decisive one (the two launcher emitters run in a different process, whose bus forwards `LAUNCHER_STAGE` alone) was not in the original finding. Added `duration_ms` end-to-end, named the game in the two launch toasts via `resolve_title`, dropped the unrenderable "Show logs" action, deleted the enum member and both validator references. See the rewritten §1.1.2. *(awaiting user device validation — V1–V5 in the plan)*
- [x] 4. ~~Make `DOWNLOAD_*` single-emitter~~ → **done, and the audit named the wrong symptom.** Deleted all 10 store-installer emits (Epic 4, Amazon 6) and unified `DOWNLOAD_PROGRESS` onto the `item=` shape, so all five events now carry the queue item. The live symptom was a **duplicate failure toast** on Epic/Amazon — one of the two with no game name — not the lost game-info refresh the audit predicted; that refresh was merely doubled with one half no-opping. Fixed three defects found in passing: the metrics cancel-timer leak, `download_duration_ms` measuring only the prefix-warmup window, and the frontend applying every progress tick to whatever row was on screen. Guarded by a narrowed `CANONICAL_SCHEMA` **plus** a new `EMITTER_OWNERS` path check in `scripts/validate_event_schemas.py`, both verified against a planted violation. See the rewritten §1.1.4. *(awaiting user device validation — TV-1…TV-8 in the plan)*
- [ ] 4a. **The circuit breaker is user-invisible and user-unresettable** *(new, found resolving item #3 — arguably the highest-value item left in this register)*. Item #3 gave the refusal a toast, which is necessary but not sufficient: `CIRCUIT_STATE_CHANGED` has no frontend subscriber (§1.3), so there is no badge telling the user the game is blocked, and `clear_launch_failures` / `arm_circuit_bypass` have no frontend caller (§1.2), so there is no way to unblock it short of waiting out the 10-minute window. Subscribe to the event and surface the two RPCs.
- [ ] 4b. **No frontend renders a toast action** *(new, found resolving item #3)*. Backend emitters build the canonical `action = {i18n_label_key, target_url, fallback_url?}` (`launcher/cloud/cloud_failure.py:_TOAST_ACTIONS`), but `boot-event-listener.tsx` and `launcherToasts.tsx` both special-case `action.verb === "retry-sync"` — a *third*, different shape — and drop everything else. So the `show-logs`, `open-save-folder`, `auth` and `disk_space_low` actions registered in `actions/unifideck_uri.py` are all unreachable. Two of them also have no target: `LaunchLogsModal` and `SaveFolderModal` exist only as translated strings in 16 locales, not as components. Either render actions generically (Decky toasts take an `onClick`, not a button) or delete the dead verbs, strings, and the `get_launch_logs` / `list_save_folder` RPCs behind them.
- [ ] 4c. **`Result.error_code` is never set on launch failures** *(new, minor, found resolving item #3)*. `_circuit_open_result()` and `handle_launcher_error()` populate `error=` and leave `error_code=None`, but `dispatcher._map_result_to_exitcode` dispatches on `error_code` exclusively — so every failure returns `GAME_FAILED` (8) and `CIRCUIT_BREAKER_OPEN` (9) plus `exit_<rc>` propagation are dead branches. Inert today because nothing reads the launcher's exit code, which is also why `ExitCode.user_message_key()` (the intended exit-code→toast mapping) has no callers. Fix the three together or delete the mapping.
- [x] 4d. ~~**`check_hardcoded_strings.py` false-positives on a positional i18n key**~~ **RESOLVED.** `launcher/proton/handlers/wrapper_clients.py:323` passed its key positionally and the checker only looks for a literal `i18n_key=` in the call span. Re-confirmed failing on a clean `HEAD` before touching it, so it was the pre-existing red, not collateral from the §1.2 pass. Fixed by naming the argument at the call site — the checker's rule is the right one.
- [ ] 4e. **Event coalescing is configured but can never run** *(new, found resolving item #4)*. `event_priority.COALESCE_KEY[DOWNLOAD_PROGRESS] = "download_id"` names a kwarg no emitter has ever sent — and it is inert regardless, because the live `EventBus.emit` writes straight to the replay buffer and never feeds `PriorityDispatcher` (`event_bus.py:86-92` says so explicitly: "every emitter in … the dispatcher's queue was never fed"). So the whole `priority_dispatcher` coalescing/back-pressure layer, its `event_bus_scaling` helper and the `coalesced_total` metric are unreachable in production. Same family as §1.3's dead wiring, but larger: decide whether the dispatcher is the emit path or delete the layer. Note the replay ring for `DOWNLOAD_PROGRESS` is only 50 deep (`event_replay.py:49`), which is the pressure coalescing was meant to relieve. **Confirmed empirically:** the 2026-08-25 bundle reports `frontend.bus_health.dispatcher` with `emitted_total: 0`, `dispatched_total: 0`, `coalesced_total: 0` on a session where events demonstrably flowed — the dispatcher is not on the emit path. See 4g for the watchdog half of the same layer.
- [ ] 4f. **Metrics has no `download_cancelled` counter** *(new, trivial, found resolving item #4)*. `counter_events` covers queued / completed / failed. A user who cancels installs is invisible in `plugin_metrics`, and cancel-vs-fail is exactly the distinction triage wants from a support bundle. One row in the table.
- [ ] 4g. **The `HandlerWatchdog` is entirely inert — nothing registers with it and nothing invokes it** *(new, found resolving §1.2)*. `invoke()` — the method holding both the per-handler timeout and the quarantine escalation — has **zero callers**, and `priority_dispatcher.py:148-150` claims the opposite: "invocation goes through the bus directly (the bus uses the watchdog internally)". It does not. `register()` is equally unreached: its only call site is `_register_with_watchdog` (`event_bus_devex.py:408`), guarded by `if watchdog is not None` in `subscribe`/`auto_wire` — and **every** `auto_wire(self, bus)` call site in the tree passes two arguments, so `watchdog` is always `None`. (A first pass of this row cited that line as proof `register()` ran; that was the §1.1.1 mistake again — an emit/call site means nothing until you check its enclosing function has callers.) Confirmed empirically rather than by grep: a Capture Logs bundle taken 2026-08-25 reports `frontend.bus_health.watchdog = {}` — zero handlers tracked — while the same bundle shows 42 events registered and a live security audit trail, so events plainly flow. Timeouts are still bounded, by the bus's own 60s `asyncio.wait_for` (`event_bus.py:387`), so this is not a hang risk; what is absent is escalation and per-handler metrics. An empty `watchdog` block in a support bundle also reads as "healthy" rather than "not wired", which is how it stayed hidden. Same decision as 4e, and probably the same change: either make the dispatcher+watchdog the real emit path or delete both layers and their bundle blocks.
- [ ] 4h. **Probe-driven quarantine would silently no-op even if probes arrived** *(new, same trace)*. `probe_reaction_service.py:135` calls `self._watchdog.force_quarantine(...)`; `HandlerWatchdog` defines `quarantine_preemptive`. The `hasattr` guard on line 122 swallows the mismatch. Same wrong-name class as §1.1.1's `app_id`/`game_id` — and doubly inert, since 4i means the handler never runs at all.
- [ ] 4i. **The runtime-probe pipeline is unbuilt end to end** *(new, same trace)*. There is no frontend CEF probe suite in `src/`, so `report_runtime_probes` was never called (confirmed by the runtime probe in §1.2), so `RUNTIME_PROBES_REPORTED` never fired (the §1.3 pass marked that member `# unwired:` pointing here), so `ProbeReactionService` and `FeatureFlagService`'s probe arms never ran, so flags stayed at defaults — and `FeatureFlagService.is_enabled` has **zero callers** anywhere, backend or frontend. The §1.2 pass deleted the two RPCs and dropped the phantom `runtime_probes` bundle key rather than fold an always-empty value. Decide the pipeline as one unit: build the probe suite and re-add the reporting route, or delete `probe_reaction_service.py`, `feature_flag_service.py`, the `PROBE_TO_*` maps and the `feature_flags` bundle block. Leaving it half-present is how it went unnoticed for a release.
- [ ] 4j. **`LaunchLogsService.export` is now callerless** *(new, bookkeeping)*. §1.2 deleted `export_launch_logs`, its only caller, because `capture_logs` already collects `launches/*.log`, `*.game.log` and `*.vendor.txt` (`support_bundle/sources.py:48-66`). `read` survives only for `get_launch_logs`, which item **4b** may delete. If 4b deletes it, the whole service goes.
- [x] **§1.3 event-bus mismatches** — 11 lines re-derived from the tree, of which **one was wrong** (`STORE_REGISTERED` has a live consumer in `store-info-store.ts`), one stale (`_config_degraded` is read by the support bundle since §1.2), one understated by an order of magnitude, and one overstated. **The two costliest defects were not in the section at all.** Playtime was billing the whole of every suspend as play time and `PlaytimeSyncService` was pushing that number to GOG/Epic — fixed by measuring off `CLOCK_MONOTONIC` rather than by adding the missing `SUSPEND`/`RESUME` emitter, which also covers hard suspends that fire nothing. A failed Game Pass check was dropping the entire xCloud library in silence while the sync bar reported success, with its explanation already translated into all 16 locales — fixed with one `SYNC_SKIPPED` subscriber and zero new strings. Retired 8 members with no future (`SUSPEND`, `RESUME`, `STORE_ERROR` — which never had an emitter in the project's whole history — the three `SUBSCRIPTION_*`, and `PLUGIN_LOADED`/`PLUGIN_UNLOADING`, found dead in both directions by the new gate); made `SHORTCUT_CREATED` live so Battle.net's sign-in tile stops rendering bare; marked 5 deliberate deferrals `# unwired:`; corrected the `CIRCUIT_STATE_CHANGED` payload docstring that register **4a** was about to build a subscriber from. New hard gate `scripts/validate_event_wiring.py` closes the class, verified against four planted violations. See the rewritten §1.3. *(awaiting user device validation — TV-1…TV-8 in the plan)*
- [x] **§1.2 dead RPC methods** — **CLOSED 2026-08-25, user-validated on device.** 29 of 102 dead (not the "roughly 13" first recorded; this section's own two lists were disjoint and nobody summed them). 24 deleted across two passes; 5 marked `# no-frontend-caller:` and deferred to 4a/4b, which own them. Three snapshots folded into the support bundle so the diagnostics outlived their routes, plus a `safe_message` field on `ValidationError` so the config-validation block cannot leak config values into a publicly-pasted bundle. `cdp/cdp_inject.py` (166 lines) went with `inject_hide_css`. The dead-RPC gate is now **hard** and asks the declared-but-unreferenced question the old one missed. Validation: runtime RPC probe before deletion (34 of 73 live methods fired, 0 of 29 suspects); V4/V5/V6 on device (Force-Compat honoured end to end — game.log shows `Running 'Proton - Experimental'`, exit 0); V7/V8 against a real Capture Logs bundle — `bus_health` 42 events, `security` 4 counters + 5 audit entries + bruteforce state, `config_validation` present, `runtime_probes` correctly absent, and zero hits across eight credential patterns in the new blocks. Four new findings split out as 4g-4j.

### P1 — consolidation (reduces drift, medium risk, needs per-store testing)

- [x] 5. ~~Promote `merge_install_status` and `_rebuild_auth_after_injection` into `shared/` as base-class hooks; delete the 3 and 5 copies.~~ **DONE, and both halves of the instruction were wrong in ways that mattered.** "The 5 copies" was 4: the fifth, Ubisoft's, is a different implementation of the same injector protocol, and deleting it would have re-opened "Auth shortcut not available" blocking sign-in. "**Base-class** hooks" would have been actively harmful — `store_injector` decides whether a store needs an auth rebuild by testing whether the attribute *exists*, so putting the hook on `StoreBase` starts calling it for Battle.net and Ubisoft; it is a mixin on the four browser-auth stores instead. The merge half found a live defect the line did not mention: Epic's and Amazon's disk-existence check was guarded on the path being truthy, so a blank path skipped it and marked a deleted game installed. Also closed the §3.4 `dir_size_bytes` bullet, and found two of the section's five bullets already resolved. New hard **check 11** pins each promoted helper to its owning module, verified against three planted violations. −164 lines of production code. See the rewritten §3.4. *(awaiting user device validation — TV-1…TV-12)*
- [x] 6. ~~Unify the three ETA parsers and three speed parsers into `shared/cli_install_helpers.py`; make GOG consume the shared helpers (and route `_resolve_gogdl_bin` through `_find_binary`).~~ **DONE, but "make GOG consume the shared helpers" was the wrong direction and was not followed.** GOG's read loop is a *superset* of `drain_install_output`, carrying the only stall watchdog in the tree; measured, Epic's and Amazon's install timeouts are unreachable while a CLI holds stdout open, so migrating GOG down would have deleted stall detection and re-opened `gog-install-100pct-hang-conditional-repair`. Lifted GOG's two-phase watchdog **into** the shared helper instead, so Epic and Amazon gain it. Parsers unified (measured identical on 23 vectors of real CLI output; the one real divergence, a negative transfer rate, was a bug in the shared and GOG copies that Amazon's guard already prevented), `amazon_progress.py` deleted, `parse_progress_line` renamed `parse_percent_re` to end the two-functions-one-name import. `_resolve_gogdl_bin` now goes through `_find_binary`, restoring the SHA256 check and the exec-bit test — not the "version check" the audit named, which has zero callers for any store. See the rewritten §3.2. *(awaiting user device validation — TV-1…TV-13 in the plan)*
- [x] 7. ~~Link the two wrapper/CLI tables: derive the wrapper predicates from a single `store_info` field so one cannot drift.~~ **DONE, in the opposite direction, and the link this asked for was the wrong one.** `uses_wine` had **zero readers anywhere**, so the two tables were a source of truth and a dead echo — and the hard check already guarding their agreement was guarding a value that could not change behaviour. Deriving *from* `store_info` is impossible as written: `wrapper_stores.py` is imported by the out-of-process launcher and `test_module_is_stdlib_only` pins it at zero imports. Removed the field instead; `get_store_infos` derives `client_runs_in_prefix` from `WRAPPER_STORES` alongside `available`, and leaving it off the dataclass means a re-added literal raises `TypeError` rather than being silently overwritten. **The hazard was the inverse of the recorded one**: `test_wrapper_stores.py` welded the three frozensets together, so the EA-App divergence this item calls sound reasoning could not happen, and forced `skips_generic_compat` means no winetricks/vcredist in that store's prefixes. Unwelded onto an explicit `_EXPECTED` table, kept the safe containment, and pinned the four dispatch maps that were *actually* unguarded (new `test_wrapper_store_dispatch_coverage.py` + **check 9** for the frontend's `CLIENT_STOREFRONTS`). Verified against 7 planted violations. See the rewritten §3.1. *(awaiting user device validation — D1–D9)*
- [x] 8. ~~Migrate Ubisoft onto `shared/prefix_clone`, `shared/wrapper_session_hooks`, and `shared/auth_shortcut`; delete the private duplicates.~~ **DONE, and two of the three named migrations were not done — deliberately, on the record.** `shared/prefix_clone`: yes, and the duplicate `rsync_clone` was hiding the real defect — no `checksum` parameter, on three call sites that repair an already-populated tree, one of which can never delete first. `shared/wrapper_session_hooks`: **declined** — `SPECS` has only a `battlenet` row, so the mixin is *inert* for Ubisoft and migrating would have swapped working behaviour for a no-op; Ubisoft's own facade is a superset. `shared/auth_shortcut`: **declined** — ~580 lines of legacy pruning, write-guard appid declarations, field self-heal and registry ops moving into `shared/` for one consumer, on the store's highest-incident path. Both declines are now written into the shared modules' docstrings rather than left to be re-filed as drift. Two defects the register line never mentioned were the actual value: **failed and cancelled Ubisoft installs were deleting UPC's own logs** (a complete `VENDOR_LOG_GLOBS` row shipped with no caller — §1.1.2's shape exactly), and **the post-play capture had no bounded wait for UPC to exit**, so a torn vault read could be propagated to every Ubisoft prefix. Also unified the bootstrap marker onto `PrefixMarker` (filename unchanged — ownership depends on it), corrected the `install_base.py` "second consumer" docstring, and recorded why `ubisoft_recovery.clone_template_into` is *not* the shared clone. Guarded by **check 10**, verified against planted violations; all 11 planted reversions of this pass's fixes were caught by its tests. See the rewritten §3.3. *(awaiting user device validation — TV-1…TV-14 in the plan; TV-11 first, it is the only one that can regress an existing install)*
- [ ] 26. **Upgrade legacy Ubisoft bootstrap markers in place** *(opened by item 8)*. The marker is JSON now, but prefixes created before that build still hold the plaintext form, which `read_marker` maps to `store=""` — so `prefix_clone.is_owned_by` reports False for them and no Ubisoft path may be moved onto it. Today that costs nothing (all five readers test `is_file()`), but it means Ubisoft cannot adopt the strict ownership test Battle.net uses. A one-shot read-and-rewrite at prefix-detection time would close it. Deliberately not folded into item 8: rewriting markers across a user's whole Ubisoft library wants its own change and its own device validation.
- [x] 9. ~~Extract the duplicated token persistence into one `shared/` primitive.~~ **DONE** — `security/token_file.EncryptedTokenFile`, consumed by both stores. Placed in `security/` not `shared/`, with reasoning in §1.4. Fixed Microsoft's `EBADF`-over-`ENOSPC` double-close in passing; guarded by `test_encrypted_token_file.py`, verified against the planted original. *(awaiting user device validation — V4, V5)*
- [x] 10. ~~Delete `StoreBase._run_cli` (dead) or make the CLI stores actually use it.~~ **DONE — deleted.** Zero callers, confirmed. Adopting it was never viable: every CLI store must *stream* stdout for progress, which a collect-at-the-end helper cannot do. A comment in its place records that, and points at `core.binaries.clean_cli_env` for the scrubbed environment it provided — the one thing worth keeping, and something all three stores already call directly. Its deletion also disproves the implication that bypassing it left GOG unscrubbed: it did not (§3.2).
- [x] 11. ~~Make microsoft's install/uninstall/update stubs return an explicit `not_supported` instead of `success=True`.~~ **DONE, and the reachability story in §3.5 was wrong in both directions.** Install and update were guarded *twice* (the `xcloud` play-section short-circuit **and** a store-name rejection inside `DownloadWorker._execute_install`, which the section did not mention); `uninstall_game` was guarded only by the frontend and is the one a caller could actually reach. All three now refuse with `error`/`error_code` `not_supported`, mapped to the existing `errors.download.generic` (zero new strings). The worker's rejection was **deleted**, not kept: it bypassed `_emit_failure`, so the row never reached "failed" and the toast echoed a hardcoded English sentence past `friendlyDownloadError`. Two phantoms went with it — the now-unreachable `microsoft` arm in `_run_prefix_warmup`, and `GameInfoCompatRow`'s `not_compatible` tag check, a tag **no backend has ever emitted**. New `usePlaySection.test.ts` pins the guard that had none, verified against a planted reordering. See the rewritten §3.5. *(awaiting user device validation — TV-5, TV-6)*
- [x] 12. ~~Resolve the `resolve_proton_path` name collision and the security-package circular-import split.~~ **DONE, and both halves were mis-filed.** The "collision" was a callerless identity stub (deleted, with its `__all__` export). The "circular import" was a cycle between two modules with **zero callers anywhere** — deleted, 750 lines — which turned out to be hiding the real finding: Epic's and Amazon's credentials sit in world-readable plaintext while GOG's and Microsoft's are 0600. Closed with `stores/shared/cli_credentials.harden_cli_credential_file` (mode, not encryption — see §1.4). The `device_fingerprint` / `device_identity` half of the line was simply wrong: that is composition, not overlap. *(awaiting user device validation — V1, V2, V3)*

### P1 follow-ups opened by the §1.4 pass

- [ ] 19. **Decide whether Epic/Amazon credentials should be encrypted at rest, not just 0600.** Item 12 removed cross-user and cross-account exposure; it did not remove same-user exposure, and every game the user launches runs as the user with `$HOME` mapped into its prefix. The deleted `ephemeral_creds` design was the right shape for this and its threat-model docstring is worth recovering from git history if this is picked up. Cost is the reason it was deferred: it wraps every legendary/nile invocation on the two most-used stores.
- [ ] 20. **Three copies of `_appid_key_candidates`** (`compatibility/library.py:350`, `core/sync_queries_mixin.py:150`, `rpc/mixins/_metadata_display.py:133`) — found while resolving §1.4's appid item and deliberately not folded in there, because they generate *both* the signed and unsigned form for a lookup rather than converting between them. Genuinely triplicated; belongs with the §3.4 cross-store copy-paste work, not with `to_unsigned`.

### P2 — documentation re-sync (zero code risk)

- [x] 13. ~~Standardize on one layer model; delete the "5-layer"/"six-layer" prose counts in `docs/architecture.md`.~~ **CLOSED 2026-08-25 — the first pass scoped itself to one file and left five.** Item 13 cleaned `architecture.md` and stopped; `README.md` (×2), `event_bus/__init__.py`, `.importlinter` and **two workflow comments the audit never listed** still restated a count. De-numbered all six and added `validate_architecture.py` **check 6**, which found the two workflow sites this register could not. §2.7 (the `.importlinter` over-claim) closes here too: `architecture.md:44` now defers to §9, and `lint-imports` confirms both contracts KEPT. Check 6 required a trailing architecture noun to avoid firing on seven correct `3-layer merge` lines in `config/`. See the rewritten §2.2. *(gate-level validation only; nothing user-visible)*
- [x] 14. ~~Correct the mixin count and complete `rpc/mixins/__init__.py.__all__` to all 20.~~ **CLOSED 2026-08-25 — the first pass fixed the code half and re-armed the doc half.** `__all__` and check 1 held through the §1.2 deletions (20 → 17); the four prose sites were hand-corrected to 20 instead of de-numbered and were stale again in the same release, plus three more sites this section's table never listed. Closed by removing the numeral everywhere and adding `validate_architecture.py` **check 5**, which fails on any mixin count written into prose (opt-out: `mixin-count-ok: <reason>`). Fixed a false positive on `Layer-6 RPC mixins` and the `tests.yml` comment that called the hard dead-RPC gate "report-only". Guarded by `tests/unit/test_validate_architecture.py`, mutation-tested. See the rewritten §2.1. *(gate-level validation only; nothing here is user-visible or deployed)*
- [x] 15. ~~Update every "five stores" reference to six; re-frame `docs/feasibility/battlenet.md` as implemented.~~ **CLOSED 2026-08-25.** The `battlenet.md` half was already done and this line was stale ("**Status: implemented.** Battle.net shipped as the sixth store"). Two real residuals fixed: `engineering-roadmap.md:79` (the evidence line for a roadmap item, understating its own scope by a store) and `CLAUDE.md:3`, whose store *list* omitted Battle.net entirely. Added **check 7**, which verifies a prose count against the store dirs rather than banning it, because a ban reported 23 correct subset statements in one run. See the rewritten §2.4. *(gate-level validation only)*
- [x] 16. Correct the StoreBase contract in `architecture.md` (10 methods, no `launch()`). *(done this audit; re-verified name by name against the ABC 2026-08-25, no drift)*
- [x] 17. ~~Remove the phantom "handler groups" docstrings and `OP-XX` markers; document the undocumented `event_bus/` and `core/sync_*` modules; add `accounts/` to the layer map.~~ **CLOSED 2026-08-25 except the OP-XX sweep, which is split out as item 21.** The docstring half named `auto_wire.py`, which is clean, and missed `download.py` and `action.py`, which were not; all three rewritten. The `accounts/` half was half-done: the layer-map row existed, but `accounts/__init__.py` still told readers the account-switch modal never appeared, which was the highest-value defect in Part 2 (a live feature, described as dead, in the file a reader opens first). Documented the missing 5 services, 14 `core/` modules and 9 `event_bus/` modules, the last with an explicit "On the emit path?" column marking the three that are not, per 4e/4g. Added **check 8** so the tables cannot drift again; it caught 25 omissions on its first run. See the rewritten §2.5. *(gate-level validation only)*
- [x] 18. Re-verify the `unifideck-architecture` skill against the current tree and bump its "Last verified" line. *(done this audit)* **Version reconciliation closed 2026-08-25 by deleting, not bumping:** `__init__.py.__version__` had zero readers anywhere while `package.json` had three, so it is gone, with the drift-guard lockstep row rewritten to forbid re-adding it. The `CLAUDE.md` H1 numeral (which contradicted line 3's own "source of truth: `package.json`") is gone too. The stale skill `Last verified:` stamps are deliberately **not** bumped: an accurate old stamp is a freshness signal, and bumping without re-reading forges it. Tracked as item 22. See the rewritten §2.6.
- [x] 20a. **Move `cleanup_sweeps.py` out of the RPC leaf.** `git mv` to `core/cleanup_sweeps.py`, no logic change, both import-linter contracts still KEPT, 89 cleanup tests green. Validated through the suite rather than by running a real wipe on the dev device. (§2.8 bullet 5.)

### Opened by the Part 2 pass

- [ ] 21. **Strip the 233 `OP-XX` markers** (149 distinct ids across 162 files). Comment-only, so one mechanical commit; kept out of the Part 2 pass so it could not drown the review. Split from item 17.
- [ ] 22. **Re-verify the six stale skill `Last verified:` stamps** (`unifideck-ci-gates`, `unifideck-dev-loop` + `debugging-playbook`, `unifideck-bug-triage`, `unifideck-release` + `unifidb-pipeline`). Real work per file: read it against the current tree, fix what drifted, *then* bump. Do not bump alone.
- [x] 23. **Wire up the launch-options parser** (§2.9). **Env half + LSFG + the tokenizer merge + the reserved return slot: DONE.** `dispatcher.py` was split at the LOC cap into `launcher/argv_options.py`. *(awaiting user device validation — V4 through V9, V12 in the plan; V4, the no-options baseline, is the one that matters)*
- [ ] 23a. **Wire `state.game_args` only, once the frontend stops leaking wrapper words into the tail** (§2.9). The deciding fact is now measured, not open: tokens after the game key do arrive as argv, so game arguments are deliverable, but the frontend's `extractUserParams` preserves the user's `mangohud`/`gamemoderun` into that same tail, and those would be passed to the game. Fix `extractUserParams` first (they are useless in the tail anyway — Steam already applied them, see §2.9 finding 2), then populate `game_args`. `test_bare_argv_tokens_would_become_game_args` is the guard.
- [ ] 23b. **Delete `ParsedOptions.wrappers` and `RuntimeState.wrappers`** (§2.9 finding 2, measured). Wrapping is a pre-exec act that Steam performs itself: `env %command% epic:Salt` ran the launcher under `env` and still delivered `argv[1] = epic:Salt`. Our launcher cannot wrap anything by the time it has an argv, so the field is unreachable, not merely unpopulated — six argv builders prepend a list that can only ever be empty. Same phantom-vocabulary class as §2.8. Touches the launch hot path for no functional gain, so it wants its own change and device validation, not a fold-in.
- [x] 24a. **`_update_existing_shortcut` wiped a user's launch options** *(found answering "how do we make LSFG support permanent", which the §2.9 wiring had just made a real question)*. It overwrote the whole `LaunchOptions` field with the canonical `"<store>:<id>"`, discarding the user's LSFG flag and per-game env variables. `_reclaim_orphan`, eleven lines below in the same file, already preserved them — the two paths disagreed, which is how it survived: whichever one a reader opened, the other was the counterexample. Fixed both to preserve user params while stripping our own `UNIFIDECK_*` flags (a stranded action flag must still be cleaned up, or a crash mid-launch permanently turns a game tile into a sign-in tile), behind a shared `rewrite_for_sync` helper. Added a protected-shortcut guard while in the area: the appid fallback that selects the entry matches on appid + `is_ours` and never consulted the protected set, and an auth forwarder is ours. 33 tests, mutation-tested against 9 planted reversions, all caught — one of which found a real bug I had introduced (the guard called `get_full_id` before string coercion).

   **Severity corrected after running a real Force Sync on device.** I first recorded this as "a Force Sync deletes your LSFG setting". The device says otherwise: a real force sync over this library reported `reconcile: 1238 games → added=1 kept=0 removed=0 reclaimed=1237`. **`kept=0` means `_update_existing_shortcut` never ran at all.** `_try_reclaim_orphan` succeeds for any game that is in the registry with a matching appid, which is every game in a settled library, so `_reclaim_orphan` is the live rewrite path — and it already preserved user params. The wipe was real code on a path that a steady-state library does not take; it is reached for a game that is not yet registered. So the user-facing loss I claimed was overstated, and the honest value of this fix is: the wipe path is closed for when it *is* taken, both paths now strip stranded flags, and protected shortcuts cannot be rewritten. The diff of `shortcuts.vdf` across that sync: 0 shortcuts changed options, 0 lost user params, 1 genuinely new game added.

   **Lesson, and it is the §1.1.1 lesson again from the other direction:** a docstring saying "only called during force sync" tells you when a function *may* run, not whether it *does*. I read that line, reproduced the wipe by calling the function directly, and wrote up a user-facing severity from it — without checking that production reaches it. The reconcile tally was one log line away the whole time.
- [ ] 25. **Steam-exported env vars are dropped by the container escape** *(measured, §2.9)*. `_forwarded_env` forwards only variables whose value differs from `os.environ`, so a variable Steam exported via `VAR=value %command%` is indistinguishable from inherited container state and goes with it. Correct as a rule, wrong for this case. Either find a way to mark user-supplied variables before the escape, or accept it and keep the docs pointing at the after-the-game-id form (which is what they now do). Low priority: the working route is documented and verified.
### Opened by the §3.1 pass

- [ ] 26. **`StoreInfo` is a mostly write-only descriptor, and the frontend type has diverged from the payload** *(found resolving §3.1)*. `uses_wine` was one of **five** unread fields, not one of one. `get_store_infos` sends `name`, `display_name`, `auth_method`, `icon_asset`, `supports_install`, `supports_cloud_saves`, `available` and now `client_runs_in_prefix`; `src/types/api.ts:153` declares `name`, `display_name`, `icon`, `available`, `auth_status` — so `icon` and `auth_status` have **never been sent** and four sent fields are undeclared. The only two anything in `src/` reads are `name` and `display_name`; `StoreConnections.tsx` and `useStoreAuth.tsx` take the icon and the label from `STORE_VISUALS` instead. Inert today, which is why it was recorded rather than swept into §3.1: nothing reads the undefined fields, so nothing breaks. But `domain.py`'s docstring says the descriptor exists "so the frontend can render the per-store badges + capability chips", which is the §1.2 DiagnosticsPanel shape — a documented consumer that was never built. Decide as one unit: build the capability chips, or shrink the descriptor and the TS interface to what is actually exchanged. Do not fix only the TS side; a type that matches an unread payload is still a lie.
- [ ] 27. **A `vulture_whitelist.py` group comment can be false for an individual member.** `uses_wine` sat under "StoreInfo fields are read through attribute access by every store" for a release with no reader anywhere. The whitelist is where a reader goes to confirm something *looks* dead but is not, so a group-level justification that is true of four names and false of the fifth is worse than no comment. The comment there now says so explicitly and names item 26. A real fix would demand a per-name reason, which is the same shape as `# no-frontend-caller:` and `# unwired:`. Adjacent to item 24 — both are the vulture gate failing to see something.
### Opened by the §3.2 pass

- [ ] 26. **`BinaryResolver.check_version` has zero callers and `CLITool.min_version` is never compared** *(found resolving §3.2 bullet 1)*. The audit cited the version check as something GOG's hardcoded path was bypassing; it is not something any store has. Unlike §1.2's dead RPCs this is a library API with a plausible caller, so it was left rather than deleted — but half-present is how the DiagnosticsPanel cluster stayed hidden for a release. Decide it as one unit: call it at resolve time and act on `min_version` (a bundled CLI older than the code expects is a real failure mode — see the `nile` 1.1.2 pin), or delete both and the `version_flag` field with them.
- [ ] 27. **Progress phases exist for GOG only** *(found resolving §3.2 bullet 3)*. `apply_dict_progress` reads `phase` / `phase_message` off any store's progress payload, and GOG uses them for "Extracting…" and "Verifying…". Epic and Amazon send neither, so their rows sit at 100% through legendary's manifest write and nile's extraction with no explanation — the same "frozen at 100%" complaint that produced GOG's phases in `gog-install-100pct-hang-conditional-repair`. Now cheap: both stores already parse through the shared helpers, so this is a phase stamp in each line handler, not new plumbing. Lower confidence than the rest of §3.2 — the tail length for legendary and nile has not been measured on device, and if it is short this is not worth doing.
- [ ] 28. **The stall message reaches the user in English** *(found resolving §3.2 bullet 2)*. A stall now surfaces as `<cli>_exit_-1: stalled: no output for 120s while downloading | <tail>`, which `friendlyDownloadError` echoes raw after stripping the prefix. That is genuinely more informative than any existing key, which is why it was left — but it is untranslated in 15 locales. Wants one new `errors.download.stalled` string with the seconds as a parameter, which is 16 manual translations (`i18n-manual-translation-no-deepl`), so it was kept out of this pass rather than done badly.

- [ ] 24. **Vulture cannot see an unimported module.** `min_confidence = 80` reports unused imports and variables, not unused functions or whole unimported modules, which is how §2.9's 198 dead lines sat behind a *hard* gate for a release. A check that every first-party module under `py_modules/unifideck` is imported by something would close it. Expect a batch of hits needing individual triage, so give it its own change.

### Opened by the §3.5 pass

- [x] 29. **Build the Battle.net `game_accounts` producer** *(§3.5 finding A — the highest user-facing item left in Part 3)*. **Landed 2026-09-14, GitHub #447.** The Wine-registry-key concern this item raised turned out not to matter: the native client's login token is a registry key, but `games-and-subs` needs a *web* session, and that's read from the shared Edge profile's CDP debugger (`Storage.getCookies`, plaintext, no on-disk cookie-store decryption needed) after an ordinary `account.battle.net` web login — the same mechanism GOG/Epic/Amazon/Microsoft already use, triggered as a background task off `BattlenetStore._on_auth_captured` so it never blocks or can fail the native-client sign-in. The response's numeric `titleId` (a different Blizzard id namespace than the PUB catalog's string `program_id`) is joined against `CatalogEntry.title_id`, already carried by the locally cached PUB catalog for exactly this purpose — no static title table needed. See `stores/battlenet/ownership/game_accounts.py`. `test_the_cache_key_the_store_reads_still_has_no_writer` deleted per its own instruction; `count_game_account_gated`'s warning stays live (still a real, useful signal for "enrichment hasn't landed *this* sign-in yet", not just the old "nothing ever writes this" case).
- [ ] 30. **The stale-sweep invariant is not machine-checked.** §3.5 finding B replaced "every registered store is sweepable" with "only stores that answered are", and `test_sync_failed_store_keeps_shortcuts.py` pins both halves — but nothing stops a future caller passing its own wider `valid_stores` to `reconcile`, which is exactly how the original defect was introduced (the guard existed and was documented; a caller overrode it). The durable form is a check that `reconcile`'s only production caller derives `valid_stores` from `_sweepable_stores`, or moving the derivation inside `reconcile` so a caller cannot widen it. Per §2.1's lesson, prefer making the wrong call impossible over testing that it is not made.
- [ ] 31. **`_ACHIEVEMENT_STORES` has an unlinked frontend twin** *(§3.5 bullet 4)*. `rpc/mixins/achievements.py:27` and `GameInfoCompatRow.tsx:195` hold the same store list in two languages with no link. Inert today (they agree), and the failure is benign in one direction — a store added to the backend only shows no button; added to the frontend only shows a button that raises `achievements_unsupported`. Same shape as §3.1's `uses_wine`, and the same remedy applies: send the capability in a payload the frontend already reads, or add a `validate_architecture.py` check. Do it with item 26 (`StoreInfo` is a mostly write-only descriptor) — that is where the capability field would live.
- [ ] 32. **Ubisoft's update path has no trigger, and GOG's DLC surface has no route** *(§3.5 bullets 3 and 4, both now marked `# unwired:`)*. Ubisoft: `check_for_updates` returns `[]`, so `update_game` → `update_op.update()` is unreachable from the UI; its own docstring says the window it opens would not render in Gaming Mode, so this is two changes, not one (source of truth for available versions, plus the watcher-based window `install_game` already uses). GOG: `get_game_dlcs` / `install_dlc` / `get_game_store_url` have zero callers, and **no store has a DLC surface** — `shared/dlc.store_supports_dlc("epic")` is a constant `True` and GOG never receives the flags. Decide each as one unit: build the missing half, or delete the manager, the methods and `shared/dlc.py` together. Left in place because both are working logic against live APIs, but half-present is how the DiagnosticsPanel cluster survived a release.
- [ ] 33. **No pre-install size and no space guard for the wrapper stores** *(§3.5 bullets 2 and 3, recorded not fixed)*. Ubisoft and Battle.net both return `None` from `get_game_size` before install, and that is **correct and unfixable locally** — neither the parsed UPC `GameConfig` nor Battle.net's `CatalogEntry` carries a size. Two consequences worth knowing rather than acting on blindly: App-Details shows `SPACE REQUIRED —`, and because `_resolve_required_bytes` exempts manual-download stores, the only free-space check on these installs is `validate_path`'s static 1 GB floor. So a user can start a 90 GB vendor-client install onto a target with 2 GB free and get whatever the vendor client does about it. If this is worth closing, the honest fix is a size hint in the storage picker rather than a fabricated `get_game_size`.

---

## Corrections — re-derived 2026-08-26

Every section of this audit is itself a claim to be validated; §1.3 says so in
as many words. These were found by re-deriving against the tree rather than
reading the register, and each one changes a decision recorded above.

### C-1. `launcher/cloud/cloud_failure.py` is dead, and this audit cites it as live twice

Verified: **zero importers**. Nothing in `py_modules/`, `bin/`, `main.py` or
`tests/` imports the module; the only textual reference in the tree is a comment
at `core/types/events.py:137`. All nine of its functions are therefore
unreachable.

Two places above rest on it:

- **Register item 4b** says "backend emitters build the canonical
  `action = {i18n_label_key, target_url, fallback_url?}`
  (`launcher/cloud/cloud_failure.py:_TOAST_ACTIONS`)". Those emitters never run.
  4b is not "the frontend drops an action the backend sends" — **neither half
  exists**. That makes 4b *cheaper*, not harder: there is no live producer to
  stay compatible with, so the action shape can be defined once on the
  `{verb, args}` side both renderers already expect.
- **§1.2 decided a behaviour was acceptable on this module's authority.** It
  states that `cloud.failure_behavior.<store>` is "read live at launch
  (`cloud_failure.py:133-141,161`) and defaults to `"toast"`, so a store whose
  cloud sync fails toasted on every launch of that game with no way to turn it
  off", and on that basis **deleted the two RPCs rather than wiring them**. The
  premise is false. The toast never fires; a cloud-save sync failure at launch is
  **silent today**. There was never a behaviour to accept, so the decision needs
  restating as a product question, not defending as recorded.

This is §1.1.1's own lesson — *"an emit site means nothing until you check the
enclosing function has callers"* — committed inside the register that names it.
It is the fourth occurrence of that class in this document.

### C-2. P0: the circuit breaker can never reset on a successful launch

`services/launch_history/service.py:129-130` reads `kwargs.get("rc")` and
`kwargs.get("elapsed")`. No emitter sends either name:

| Emit site | Sends |
|---|---|
| `services/launcher/orchestrator.py:143` | `store`, `game_id` |
| `services/launcher/orchestrator.py:200` | `store`, `game_id` |
| `services/launcher/service.py:440` | `store`, `game_id` |
| `rpc/mixins/launch.py:105` | `store`, `game_id`, `app_id`, `exit_code` |

`CANONICAL_SCHEMA` declares `exit_code` / `elapsed_seconds`. So `rc` is always
`None`, `_on_game_stopped` returns at its `if rc is None` guard on every stop,
**`record_success` has no reachable call site anywhere in the tree**, and
`FAILURE_KIND_FAST_BOOT` is never recorded. The breaker accumulates only
`launcher_error` failures (`services/launcher/error_toasts.py:79`) and can be
cleared only by the 600s window or by `clear_launch_failures`, which has no
frontend caller.

Consequence for register item 4a: a badge built today would show failures that
can never clear. **Fix this first.** Tracked as item 46.

Nothing could have caught it — `validate_event_schemas.py` is emit-side only.
A subscribe-side arm finds exactly three mismatches across 41 subscribers: this
one, and the two dead fallbacks in C-5.

### C-3. Item 21's count is right; its method is wrong

Excluding `docs/archive/`, the recorded 233 / 149 / 162 is accurate (measured
234 / 149 / 164). The whole-tree figure of 387 includes an archive file that must
**not** be swept. A correct regex gives **156** distinct ids — `OP-[0-9]+[a-z]?`
misses the `OP-08l-bis`, `OP-08c1`, `OP-14c-bis` forms.

But "comment-only, so a single mechanical commit" is misleading and a line-delete
sweep would corrupt docstrings: roughly **160 are banner lines inside module
docstrings**, not `#` comments, and roughly **69 are mid-sentence prose inside
docstrings** (`stores/gog/store.py:10-17`, `services/artwork/fetcher.py:262`,
`rpc/wrapper.py:135`), which need the parenthetical removed rather than the line.
Zero occurrences sit in executable code.

### C-4. Item 22's premise fails for its most important file

`unifideck-ci-gates/SKILL.md` is stamped `2026-07-02 / v0.7.0`, yet its body
already documents `scripts/validate_event_wiring.py` and the
`CLIENT_STOREFRONTS ↔ WRAPPER_STORES` check — created 2026-08-25 and 2026-08-26.
The content tracks the tree; only the bump was skipped. Item 18's reasoning
("an accurate old stamp is a freshness signal, and bumping without re-reading
forges it") does not apply here: this stamp is wrong in the *safe* direction,
which is still wrong, and it will send the next reader to re-verify a file that
needs nothing.

Genuinely stale in both content and stamp: **only**
`unifideck-release/unifidb-pipeline.md`.

Two files item 22 does not list have **no `Last verified:` line at all** —
`unifideck-drift-guard/SKILL.md` and `unifideck-bug-triage/triage-playbook.md` —
while `docs/engineering-roadmap.md` item 8 claims the stamps exist on all skills.
The class here is not "six stamps are old"; it is that **nothing enforces that a
stamp exists or moves**.

### C-5. Two dead defensive fallbacks, and a shadow-package trap

- `services/compatibility/service.py:133-134` and
  `services/artwork/event_handlers.py:226` read flat `games` / `is_force` off
  `POST_SYNC_PHASE_CHANGED` "for emitters that haven't been migrated yet". No
  such emitter exists. Harmless — the `sync_kwargs` primary path is correct — but
  it is phantom-compat vocabulary of the §2.8 class. Tracked as item 41.
- **`launcher/fixes/` and `launcher/language_setup/` shadowed the real
  `launcher/proton/fixes/` and `launcher/proton/language_setup/`** with 5-of-5
  and 5-of-7 identical module names, all empty
  (`# TODO: implement — see operational plan PDF`), untouched since the initial
  commit. `from unifideck.launcher.fixes.game_fixes import …` resolved, imported
  nothing and raised nothing — the silent mis-resolve of §1.4's
  `resolve_proton_path`, at package scale. Deleted 2026-08-26 (item 34). Vulture
  could not see them: at `min_confidence = 80` it reports neither unused
  functions nor whole unimported modules, which is register item 24.

### Not a defect — recorded so it is not re-filed

`SUPPORTED_STORES = {gog, amazon, epic}`
(`src/lib/steam-bridge/app-context-menu-patch.ts:32`) versus
`_DIRECT_LAUNCH_STORES = frozenset({"gog", "amazon"})`
(`rpc/mixins/executable.py:44`) reads as a TS/Python disagreement of the §3.1
class. It is not. They encode different concepts: the Python set is "stores whose
override is written into and read back from the `games.map` row", and Epic's
override goes through `legendary --override-exe` from the config key instead —
as the comment at `executable.py:41-43` says. Both lists are correct.
